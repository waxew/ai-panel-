/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";
// راهنما: این دستور متغیر/ثابت «cors» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, POST, OPTIONS"};
// راهنما: این دستور متغیر/ثابت «json» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const json=(data:unknown,status=200)=>Response.json(data,{status,headers:{...cors,"Cache-Control":"no-store"}});
// راهنما: این تابع «membership» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function membership(admin:any,userId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data,error}=await admin.from("WorkspaceMember").select("workspaceId,role").eq("userId",userId).limit(1);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data?.[0] as {workspaceId:string;role:string}|undefined» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data?.[0] as {workspaceId:string;role:string}|undefined;}
// راهنما: این تابع «staffAccess» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function staffAccess(admin:any,workspaceId:string,userId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data,error}=await admin.from("BookingStaffAccess").select("staffId,isEnabled,appointmentScope,permissions").eq("workspaceId",workspaceId).eq("userId",userId).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data;}
// راهنما: این تابع «snapshot» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function snapshot(admin:any,workspaceId:string,staffFilter?:string){
  // راهنما: این دستور متغیر/ثابت «appointmentsQuery» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const appointmentsQuery=admin.from("BookingAppointment").select("id,customerId,serviceId,staffId,startsAt,status,amount,paidAmount,currency,BookingCustomer(fullName,phone),BookingService(title),BookingStaff(name)").eq("workspaceId",workspaceId).order("startsAt",{ascending:false}).limit(1000);/* راهنما: این شرط بررسی می‌کند آیا «staffFilter» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(staffFilter)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «appointmentsQuery.eq("staffId",staffFilter)». */ appointmentsQuery.eq("staffId",staffFilter);
  // راهنما: این دستور متغیر/ثابت «[payments,expenses,appointments,services,sta…» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const [payments,expenses,appointments,services,staff]=await Promise.all([
    admin.from("BookingPayment").select("id,appointmentId,customerId,type,method,amount,currency,reference,note,status,paidAt,createdAt,BookingCustomer(fullName,phone),BookingAppointment(startsAt,staffId,amount,paidAmount,BookingService(title),BookingStaff(name))").eq("workspaceId",workspaceId).order("paidAt",{ascending:false}).limit(500),
    admin.from("BookingExpense").select("id,category,amount,currency,vendor,note,status,occurredAt,createdAt").eq("workspaceId",workspaceId).order("occurredAt",{ascending:false}).limit(500),
    appointmentsQuery,
    admin.from("BookingService").select("id,title,color").eq("workspaceId",workspaceId),
    admin.from("BookingStaff").select("id,name,color").eq("workspaceId",workspaceId),
  ]);
  // راهنما: این دستور متغیر/ثابت «error» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const error=[payments.error,expenses.error,appointments.error,services.error,staff.error].find(Boolean);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;
  // راهنما: این دستور متغیر/ثابت «aps» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const aps=appointments.data??[];/* راهنما: این دستور متغیر/ثابت «allowedIds» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const allowedIds=new Set(aps.map((a:any)=>a.id));
  // راهنما: این دستور متغیر/ثابت «ps» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const ps=(payments.data??[]).filter((p:any)=>!staffFilter||allowedIds.has(p.appointmentId));
  // راهنما: این دستور متغیر/ثابت «es» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const es=staffFilter?[]:(expenses.data??[]);
  // راهنما: این دستور متغیر/ثابت «postedPayments» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const postedPayments=ps.filter((p:any)=>p.status==='POSTED');
  // راهنما: این دستور متغیر/ثابت «income» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const income=postedPayments.reduce((s:number,p:any)=>s+(p.type==='PAYMENT'?Number(p.amount):-Number(p.amount)),0);
  // راهنما: این دستور متغیر/ثابت «expense» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const expense=es.filter((e:any)=>e.status==='POSTED').reduce((s:number,e:any)=>s+Number(e.amount),0);
  // راهنما: این دستور متغیر/ثابت «receivable» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const receivable=aps.filter((a:any)=>a.status!=='CANCELLED').reduce((s:number,a:any)=>s+Math.max(0,Number(a.amount)-Number(a.paidAmount)),0);
  // راهنما: این دستور متغیر/ثابت «now» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const now=new Date();/* راهنما: این دستور متغیر/ثابت «monthStart» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const monthStart=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),1));
  // راهنما: این دستور متغیر/ثابت «monthIncome» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const monthIncome=postedPayments.filter((p:any)=>new Date(p.paidAt)>=monthStart).reduce((s:number,p:any)=>s+(p.type==='PAYMENT'?Number(p.amount):-Number(p.amount)),0);
  // راهنما: این دستور متغیر/ثابت «monthExpense» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const monthExpense=es.filter((e:any)=>e.status==='POSTED'&&new Date(e.occurredAt)>=monthStart).reduce((s:number,e:any)=>s+Number(e.amount),0);
  // راهنما: این دستور متغیر/ثابت «byService» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const byService=(services.data??[]).map((sv:any)=>{/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=aps.filter((a:any)=>a.serviceId===sv.id);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «{id:sv.id,title:sv.title,color:sv.color,count:rows.length,revenue:rows.red…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return{id:sv.id,title:sv.title,color:sv.color,count:rows.length,revenue:rows.reduce((s:number,a:any)=>s+Number(a.paidAmount||0),0)};}).sort((a:any,b:any)=>b.revenue-a.revenue);
  // راهنما: این دستور متغیر/ثابت «byStaff» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const byStaff=(staff.data??[]).filter((st:any)=>!staffFilter||st.id===staffFilter).map((st:any)=>{/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=aps.filter((a:any)=>a.staffId===st.id);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «{id:st.id,name:st.name,color:st.color,count:rows.length,revenue:rows.reduc…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return{id:st.id,name:st.name,color:st.color,count:rows.length,revenue:rows.reduce((s:number,a:any)=>s+Number(a.paidAmount||0),0)};}).sort((a:any,b:any)=>b.revenue-a.revenue);
  // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «{ok:true,summary:{income,expense,net:income-expense,receivable,monthIncome…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
  return{ok:true,summary:{income,expense,net:income-expense,receivable,monthIncome,monthExpense,monthNet:monthIncome-monthExpense},payments:ps,expenses:es,appointments:aps,byService,byStaff};
}
// راهنما: این دستور متغیر/ثابت «handler» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const handler=withSupabase({auth:"user"},async(req,ctx)=>{
  // راهنما: این دستور متغیر/ثابت «userId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const userId=ctx.userClaims?.id;/* راهنما: این شرط بررسی می‌کند آیا «!userId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!userId)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ورود به حساب الزامی است."},401)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ورود به حساب الزامی است."},401);/* راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «member» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const member=await membership(admin,userId);/* راهنما: این شرط بررسی می‌کند آیا «!member» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!member)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"فضای کاری پیدا نشد."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"فضای کاری پیدا نشد."},404);
  // راهنما: این دستور متغیر/ثابت «isAdmin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const isAdmin=member.role==='ADMIN'||member.role==='SUPER_ADMIN';/* راهنما: این دستور متغیر/ثابت «access» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let access:any=null;/* راهنما: این شرط بررسی می‌کند آیا «!isAdmin» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!isAdmin){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «access=await staffAccess(admin,member.workspaceId,userId)». */ access=await staffAccess(admin,member.workspaceId,userId);/* راهنما: این شرط بررسی می‌کند آیا «!access?.isEnabled» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!access?.isEnabled)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"دسترسی پرسنلی شما غیرفعال است."},403)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"دسترسی پرسنلی شما غیرفعال است."},403);}
  // راهنما: این دستور متغیر/ثابت «mode» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const mode=new URL(req.url).searchParams.get('mode')==='reports'?'reports':'finance';
  // راهنما: این شرط بررسی می‌کند آیا «req.method==='GET'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(req.method==='GET'){
    // راهنما: این دستور متغیر/ثابت «permitted» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
    const permitted=isAdmin||(mode==='reports'?access?.permissions?.reports===true:access?.permissions?.finance===true);/* راهنما: این شرط بررسی می‌کند آیا «!permitted» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!permitted)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"برای مشاهده این بخش دسترسی ندارید."},403)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"برای مشاهده این بخش دسترسی ندارید."},403);
    // راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود.
    try{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentS…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentScope==='OWN'?access.staffId:undefined));}catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error(e)». */ console.error(e);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"گزارش مالی در دسترس نیست."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"گزارش مالی در دسترس نیست."},500)}
  }
  // راهنما: این شرط بررسی می‌کند آیا «req.method!=='POST'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(req.method!=='POST')/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Method not allowed"},405)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Method not allowed"},405);/* راهنما: این شرط بررسی می‌کند آیا «!isAdmin&&access?.permissions?.finance!==true» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!isAdmin&&access?.permissions?.finance!==true)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"مجوز عملیات مالی ندارید."},403)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"مجوز عملیات مالی ندارید."},403);
  // راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  let body:any;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body=await req.json()». */ body=await req.json()}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"درخواست معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"درخواست معتبر نیست."},400)}
  // راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود.
  try{
    // راهنما: این شرط بررسی می‌کند آیا «body.action==='add_payment'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(body.action==='add_payment'){
      // راهنما: این دستور متغیر/ثابت «appointmentId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
      const appointmentId=typeof body.appointmentId==='string'?body.appointmentId:'';/* راهنما: این دستور متغیر/ثابت «amount» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const amount=Number(body.amount);/* راهنما: این دستور متغیر/ثابت «method» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const method=['CASH','CARD','POS','TRANSFER','OTHER'].includes(body.method)?body.method:'CASH';/* راهنما: این شرط بررسی می‌کند آیا «!appointmentId||!Number.isSafeInteger(amount)||amount<=0» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!appointmentId||!Number.isSafeInteger(amount)||amount<=0)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"مبلغ یا نوبت معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"مبلغ یا نوبت معتبر نیست."},400);
      // راهنما: این دستور متغیر/ثابت «q» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
      let q=admin.from("BookingAppointment").select("id,workspaceId,staffId,amount,paidAmount").eq("id",appointmentId).eq("workspaceId",member.workspaceId);/* راهنما: این شرط بررسی می‌کند آیا «!isAdmin&&access.appointmentScope==='OWN'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!isAdmin&&access.appointmentScope==='OWN')/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «q=q.eq("staffId",access.staffId)». */ q=q.eq("staffId",access.staffId);/* راهنما: این دستور متغیر/ثابت «{data:appt,error:ae}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:appt,error:ae}=await q.maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «ae» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(ae)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw ae;/* راهنما: این شرط بررسی می‌کند آیا «!appt» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!appt)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"نوبت پیدا نشد یا خارج از محدوده دسترسی است."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"نوبت پیدا نشد یا خارج از محدوده دسترسی است."},404);/* راهنما: این دستور متغیر/ثابت «remaining» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const remaining=Math.max(0,Number(appt.amount)-Number(appt.paidAmount));/* راهنما: این شرط بررسی می‌کند آیا «amount>remaining» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(amount>remaining)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"مبلغ پرداخت از مانده نوبت بیشتر است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"مبلغ پرداخت از مانده نوبت بیشتر است."},400);
      // راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
      const{error}=await admin.from("BookingPayment").insert({workspaceId:member.workspaceId,appointmentId,customerId:'',amount,method,type:'PAYMENT',reference:typeof body.reference==='string'?body.reference.trim().slice(0,120)||null:null,note:typeof body.note==='string'?body.note.trim().slice(0,500)||null:null,paidAt:body.paidAt?new Date(body.paidAt).toISOString():new Date().toISOString(),createdByUserId:userId});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentS…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentScope==='OWN'?access.staffId:undefined),201);
    }
    // راهنما: این شرط بررسی می‌کند آیا «body.action==='add_refund'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(body.action==='add_refund'){
      // راهنما: این دستور متغیر/ثابت «appointmentId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
      const appointmentId=typeof body.appointmentId==='string'?body.appointmentId:'';/* راهنما: این دستور متغیر/ثابت «amount» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const amount=Number(body.amount);/* راهنما: این شرط بررسی می‌کند آیا «!appointmentId||!Number.isSafeInteger(amount)||amount<=0» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!appointmentId||!Number.isSafeInteger(amount)||amount<=0)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات بازپرداخت معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات بازپرداخت معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «q» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let q=admin.from("BookingAppointment").select("id,staffId,paidAmount").eq("id",appointmentId).eq("workspaceId",member.workspaceId);/* راهنما: این شرط بررسی می‌کند آیا «!isAdmin&&access.appointmentScope==='OWN'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!isAdmin&&access.appointmentScope==='OWN')/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «q=q.eq("staffId",access.staffId)». */ q=q.eq("staffId",access.staffId);/* راهنما: این دستور متغیر/ثابت «{data:appt}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:appt}=await q.maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «!appt» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!appt)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"نوبت پیدا نشد."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"نوبت پیدا نشد."},404);/* راهنما: این شرط بررسی می‌کند آیا «amount>Number(appt.paidAmount)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(amount>Number(appt.paidAmount))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"مبلغ بازپرداخت از مبلغ پرداخت‌شده بیشتر است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"مبلغ بازپرداخت از مبلغ پرداخت‌شده بیشتر است."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingPayment").insert({workspaceId:member.workspaceId,appointmentId,customerId:'',amount,type:'REFUND',method:'OTHER',note:typeof body.note==='string'?body.note.trim().slice(0,500)||null:null,createdByUserId:userId});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentS…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentScope==='OWN'?access.staffId:undefined),201);
    }
    // راهنما: این شرط بررسی می‌کند آیا «body.action==='void_payment'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(body.action==='void_payment'){
      // راهنما: این دستور متغیر/ثابت «id» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
      const id=typeof body.id==='string'?body.id:'';/* راهنما: این شرط بررسی می‌کند آیا «!id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!id)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"شناسه تراکنش معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"شناسه تراکنش معتبر نیست."},400);/* راهنما: این شرط بررسی می‌کند آیا «!isAdmin&&access.appointmentScope==='OWN'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!isAdmin&&access.appointmentScope==='OWN'){/* راهنما: این دستور متغیر/ثابت «{data:p}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:p}=await admin.from("BookingPayment").select("id,BookingAppointment!inner(staffId)").eq("id",id).eq("workspaceId",member.workspaceId).eq("BookingAppointment.staffId",access.staffId).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «!p» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!p)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"تراکنش خارج از محدوده دسترسی است."},403)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"تراکنش خارج از محدوده دسترسی است."},403);}/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingPayment").update({status:'VOID'}).eq("id",id).eq("workspaceId",member.workspaceId).eq("status",'POSTED');/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentS…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await snapshot(admin,member.workspaceId,!isAdmin&&access.appointmentScope==='OWN'?access.staffId:undefined));
    }
    // راهنما: این شرط بررسی می‌کند آیا «body.action==='add_expense'||body.action==='void_expense'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(body.action==='add_expense'||body.action==='void_expense'){/* راهنما: این شرط بررسی می‌کند آیا «!isAdmin» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!isAdmin)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ثبت و ابطال هزینه فقط برای مدیر مجاز است."},403)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ثبت و ابطال هزینه فقط برای مدیر مجاز است."},403);}
    // راهنما: این شرط بررسی می‌کند آیا «body.action==='add_expense'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(body.action==='add_expense'){/* راهنما: این دستور متغیر/ثابت «amount» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const amount=Number(body.amount);/* راهنما: این دستور متغیر/ثابت «category» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const category=typeof body.category==='string'?body.category.trim().slice(0,100):'';/* راهنما: این شرط بررسی می‌کند آیا «!category||!Number.isSafeInteger(amount)||amount<=0» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!category||!Number.isSafeInteger(amount)||amount<=0)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات هزینه معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات هزینه معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingExpense").insert({workspaceId:member.workspaceId,category,amount,vendor:typeof body.vendor==='string'?body.vendor.trim().slice(0,160)||null:null,note:typeof body.note==='string'?body.note.trim().slice(0,500)||null:null,occurredAt:body.occurredAt?new Date(body.occurredAt).toISOString():new Date().toISOString(),createdByUserId:userId});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await snapshot(admin,member.workspaceId),201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await snapshot(admin,member.workspaceId),201);}
    // راهنما: این شرط بررسی می‌کند آیا «body.action==='void_expense'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(body.action==='void_expense'){/* راهنما: این دستور متغیر/ثابت «id» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const id=typeof body.id==='string'?body.id:'';/* راهنما: این شرط بررسی می‌کند آیا «!id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!id)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"شناسه هزینه معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"شناسه هزینه معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingExpense").update({status:'VOID',updatedAt:new Date().toISOString()}).eq("id",id).eq("workspaceId",member.workspaceId).eq("status",'POSTED');/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await snapshot(admin,member.workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await snapshot(admin,member.workspaceId));}
    // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"عملیات مالی شناخته‌شده نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
    return json({ok:false,message:"عملیات مالی شناخته‌شده نیست."},400);
  }catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error('booking-finance',e)». */ console.error('booking-finance',e);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"عملیات مالی انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"عملیات مالی انجام نشد."},500)}
});
// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{async fetch(req:Request){/* راهنما: این شرط بررسی می‌کند آیا «req.method==='OPTIONS'» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(req.method==='OPTIONS')/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response('ok',{headers:cors})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response('ok',{headers:cors});/* راهنما: این دستور متغیر/ثابت «res» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const res=await handler(req);/* راهنما: این دستور متغیر/ثابت «h» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const h=new Headers(res.headers);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «Object.entries(cors).forEach(([k,v])=>h.set(k,v))». */ Object.entries(cors).forEach(([k,v])=>h.set(k,v));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response(res.body,{status:res.status,headers:h})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response(res.body,{status:res.status,headers:h});}};
