/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";

// راهنما: این Type با نام «Btn» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Btn={id:string;parentId?:string|null;title:string;actionType:string;actionValue?:string|null;sortOrder:number};
// راهنما: این Type با نام «Msg» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Msg={message_id?:string;text?:string;sender_id?:string;sender_type?:string;aux_data?:{button_id?:string}};
// راهنما: این Type با نام «Update» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Update={type?:string;chat_id?:string;new_message?:Msg;updated_message?:Msg};
// راهنما: این دستور متغیر/ثابت «money» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const money=(v:any,c="IRR")=>`${new Intl.NumberFormat("fa-IR").format(Number(v??0))} ${c==="IRR"?"ریال":c}`;
// راهنما: این تابع «hex» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function hex(b:Uint8Array){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «Array.from(b,x=>x.toString(16).padStart(2,"0")).join("")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return Array.from(b,x=>x.toString(16).padStart(2,"0")).join("")}
// راهنما: این تابع «sha256» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function sha256(v:string){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «hex(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return hex(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v))))}
// راهنما: این تابع «fromHex» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function fromHex(v:string){/* راهنما: این شرط بررسی می‌کند آیا «!/^[0-9a-f]{64}$/i.test(v)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!/^[0-9a-f]{64}$/i.test(v))/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error("bad_key");/* راهنما: این دستور متغیر/ثابت «b» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const b=new Uint8Array(v.length/2);/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(let i=0;i<b.length;i++)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «b[i]=parseInt(v.slice(i*2,i*2+2),16)». */ b[i]=parseInt(v.slice(i*2,i*2+2),16);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «b» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return b}
// راهنما: این تابع «from64» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function from64(v:string){/* راهنما: این دستور متغیر/ثابت «s، b» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const s=atob(v),b=new Uint8Array(s.length);/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(let i=0;i<s.length;i++)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «b[i]=s.charCodeAt(i)». */ b[i]=s.charCodeAt(i);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «b» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return b}
// راهنما: این تابع «decrypt» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function decrypt(c:string,k:string){/* راهنما: این دستور متغیر/ثابت «[v,iv,p]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [v,iv,p]=c.split(":");/* راهنما: این شرط بررسی می‌کند آیا «v!=="v1"||!iv||!p» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(v!=="v1"||!iv||!p)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error("bad_cipher");/* راهنما: این دستور متغیر/ثابت «key» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const key=await crypto.subtle.importKey("raw",fromHex(k),{name:"AES-GCM"},false,["decrypt"]);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new TextDecoder().decode(await crypto.subtle.decrypt({name:"AES-GCM",iv:fr…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new TextDecoder().decode(await crypto.subtle.decrypt({name:"AES-GCM",iv:from64(iv)},key,from64(p)))}
// راهنما: این تابع «rb» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function rb(token:string,method:string,body:Record<string,unknown>){/* راهنما: این متغیر «r» نتیجه یا Promise یک درخواست شبکه را نگه می‌دارد. */ const r=await fetch(`https://botapi.rubika.ir/v3/${encodeURIComponent(token)}/${method}`,{method:"POST",headers:{"content-type":"application/json",accept:"application/json"},body:JSON.stringify(body)});/* راهنما: این شرط بررسی می‌کند آیا «!r.ok» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!r.ok)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("rubika",method,r.status)». */ console.error("rubika",method,r.status);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «r» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return r}
// راهنما: این تابع «rows» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function rows(buttons:Array<{id:string;title:string}>){/* راهنما: این دستور متغیر/ثابت «out» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const out:any[]=[];/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(let i=0;i<buttons.length;i+=2)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «out.push({buttons:buttons.slice(i,i+2).map(b=>({id:b.id,type:"Simple",button_text:b.title…». */ out.push({buttons:buttons.slice(i,i+2).map(b=>({id:b.id,type:"Simple",button_text:b.title}))});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «out» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return out}
// راهنما: این تابع «send» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function send(token:string,chatId:string,text:string,buttons:Array<{id:string;title:string}>=[]){/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const body:any={chat_id:chatId,text};/* راهنما: این شرط بررسی می‌کند آیا «buttons.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttons.length){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body.chat_keypad_type="New"». */ body.chat_keypad_type="New";/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body.chat_keypad={rows:rows(buttons),resize_keyboard:true,on_time_keyboard:false}». */ body.chat_keypad={rows:rows(buttons),resize_keyboard:true,on_time_keyboard:false}}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «rb(token,"sendMessage",body)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return rb(token,"sendMessage",body)}
// راهنما: این تابع «storeFor» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function storeFor(admin:any,workspaceId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.from("Store").select("id,name,currency,status").eq("workspaceId",workspaceId).eq("status","ACTIVE").maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data}
// راهنما: این تابع «roots» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function roots(buttons:Btn[]){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «buttons.filter(b=>!b.parentId).sort((a,b)=>a.sortOrder-b.sortOrder)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return buttons.filter(b=>!b.parentId).sort((a,b)=>a.sortOrder-b.sortOrder)}
// راهنما: این تابع «children» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function children(buttons:Btn[],id:string){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «buttons.filter(b=>b.parentId===id).sort((a,b)=>a.sortOrder-b.sortOrder)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return buttons.filter(b=>b.parentId===id).sort((a,b)=>a.sortOrder-b.sortOrder)}
// راهنما: این تابع «catalog» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function catalog(admin:any,token:string,chatId:string,store:any){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.from("StoreItem").select("id,title,priceAmount,currency,inventoryCount").eq("storeId",store.id).eq("isActive",true).order("sortOrder").limit(20);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این دستور متغیر/ثابت «items» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const items=data??[];/* راهنما: این شرط بررسی می‌کند آیا «!items.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!items.length)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,`فروشگاه «${store.name}» هنوز محصول فعالی ندارد.`)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,`فروشگاه «${store.name}» هنوز محصول فعالی ندارد.`);/* راهنما: این دستور متغیر/ثابت «text» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const text=[`🛍 محصولات «${store.name}»`,...items.map((x:any,i:number)=>`${i+1}. ${x.title} — ${money(x.priceAmount,x.currency)}${x.inventoryCount!==null&&Number(x.inventoryCount)<=0?" — ناموجود":""}`)].join("\n");/* راهنما: این دستور متغیر/ثابت «btns» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const btns=items.filter((x:any)=>x.inventoryCount===null||Number(x.inventoryCount)>0).map((x:any)=>({id:`add:${x.id}`,title:`➕ ${x.title}`}));/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «btns.push({id:"cart:view",title:"🛒 مشاهده سبد"})». */ btns.push({id:"cart:view",title:"🛒 مشاهده سبد"});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,text,btns)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,text,btns)}
// راهنما: این تابع «cart» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function cart(admin:any,token:string,chatId:string,store:any,senderId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.rpc("channel_cart_snapshot",{p_platform:"rubika",p_store_id:store.id,p_external_user_id:senderId});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این دستور متغیر/ثابت «c» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const c=data??{};/* راهنما: این دستور متغیر/ثابت «items» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const items=Array.isArray(c.items)?c.items:[];/* راهنما: این شرط بررسی می‌کند آیا «!items.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!items.length)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,"🛒 سبد خرید شما خالی است.",[{id:"catalog:view",title:"�…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,"🛒 سبد خرید شما خالی است.",[{id:"catalog:view",title:"🛍 محصولات"}]);/* راهنما: این دستور متغیر/ثابت «text» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const text=["🛒 سبد خرید",...items.map((x:any)=>`${x.title} × ${x.quantity} — ${money(x.lineTotalAmount,x.currency)}`),`\nجمع: ${money(c.totalAmount,c.currency)}`].join("\n");/* راهنما: این دستور متغیر/ثابت «btns» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const btns:any[]=[];/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(const x of items){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «btns.push({id:`dec:${x.itemId}`,title:`➖ ${x.title}`},{id:`inc:${x.itemId}`,title:`➕ ${x.…». */ btns.push({id:`dec:${x.itemId}`,title:`➖ ${x.title}`},{id:`inc:${x.itemId}`,title:`➕ ${x.title}`})}/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «btns.push({id:"checkout",title:"✅ ثبت سفارش"},{id:"catalog:view",title:"🛍 ادامه خرید"})». */ btns.push({id:"checkout",title:"✅ ثبت سفارش"},{id:"catalog:view",title:"🛍 ادامه خرید"});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,text,btns)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,text,btns)}
// راهنما: این تابع «changeCart» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function changeCart(admin:any,token:string,chatId:string,store:any,senderId:string,itemId:string,delta:number){/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {error}=await admin.rpc("channel_cart_change",{p_platform:"rubika",p_store_id:store.id,p_item_id:itemId,p_external_user_id:senderId,p_username:null,p_display_name:null,p_delta:delta});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error){/* راهنما: این دستور متغیر/ثابت «m» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const m=String(error.message||"");/* راهنما: این شرط بررسی می‌کند آیا «m.includes("insufficient_stock")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(m.includes("insufficient_stock"))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,"موجودی این محصول کافی نیست.")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,"موجودی این محصول کافی نیست.");/* راهنما: این شرط بررسی می‌کند آیا «m.includes("item_unavailable")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(m.includes("item_unavailable"))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,"این محصول دیگر در دسترس نیست.")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,"این محصول دیگر در دسترس نیست.");/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «cart(admin,token,chatId,store,senderId)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return cart(admin,token,chatId,store,senderId)}
// راهنما: این تابع «checkout» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function checkout(admin:any,token:string,chatId:string,store:any,senderId:string,msgId?:string){/* راهنما: این دستور متغیر/ثابت «key» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const key=`rubika:${store.id}:${senderId}:${msgId||crypto.randomUUID()}`;/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.rpc("channel_checkout_cart",{p_platform:"rubika",p_store_id:store.id,p_external_user_id:senderId,p_external_conversation_id:chatId,p_idempotency_key:key});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error){/* راهنما: این دستور متغیر/ثابت «m» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const m=String(error.message||"");/* راهنما: این شرط بررسی می‌کند آیا «m.includes("cart_empty")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(m.includes("cart_empty"))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,"سبد خرید خالی است.")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,"سبد خرید خالی است.");/* راهنما: این شرط بررسی می‌کند آیا «m.includes("cart_items_unavailable")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(m.includes("cart_items_unavailable"))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,"بعضی محصولات سبد دیگر موجود نیستند؛ سبد را دوباره بررسی…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,"بعضی محصولات سبد دیگر موجود نیستند؛ سبد را دوباره بررسی کنید.");/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,`✅ سفارش ثبت شد.\nشماره سفارش: #${String(data.orderId).s…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,`✅ سفارش ثبت شد.\nشماره سفارش: #${String(data.orderId).slice(0,8)}\nمبلغ: ${money(data.totalAmount,data.currency)}\nوضعیت: در انتظار پرداخت`,[{id:"orders:view",title:"📦 سفارش‌های من"},{id:"catalog:view",title:"🛍 خرید بیشتر"}])}
// راهنما: این تابع «orders» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function orders(admin:any,token:string,chatId:string,store:any,senderId:string){/* راهنما: این دستور متغیر/ثابت «{data:c,error:ce}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:c,error:ce}=await admin.from("StoreCustomer").select("id").eq("storeId",store.id).eq("platform","rubika").eq("externalUserId",senderId).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «ce» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(ce)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw ce;/* راهنما: این شرط بررسی می‌کند آیا «!c» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!c)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,"هنوز سفارشی ثبت نکرده‌اید.")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,"هنوز سفارشی ثبت نکرده‌اید.");/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.from("StoreOrder").select("id,status,totalAmount,currency,createdAt").eq("storeId",store.id).eq("customerId",c.id).order("createdAt",{ascending:false}).limit(8);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این دستور متغیر/ثابت «list» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const list=data??[];/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,list.length?["📦 سفارش‌های شما",...list.map((x:any)=>`#$…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,list.length?["📦 سفارش‌های شما",...list.map((x:any)=>`#${x.id.slice(0,8)} — ${x.status} — ${money(x.totalAmount,x.currency)}`)].join("\n"):"هنوز سفارشی ندارید.")}
// راهنما: این تابع «action» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function action(admin:any,token:string,chatId:string,store:any,senderId:string,msgId:string|undefined,b:Btn,all:Btn[]){/* راهنما: این شرط بررسی می‌کند آیا «b.actionType==="CATALOG"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b.actionType==="CATALOG")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «store?catalog(admin,token,chatId,store):send(token,chatId,"فروشگاه فعال نی…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return store?catalog(admin,token,chatId,store):send(token,chatId,"فروشگاه فعال نیست.");/* راهنما: این شرط بررسی می‌کند آیا «b.actionType==="CART"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b.actionType==="CART")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «store?cart(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه ف…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return store?cart(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه فعال نیست.");/* راهنما: این شرط بررسی می‌کند آیا «b.actionType==="ORDERS"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b.actionType==="ORDERS")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «store?orders(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return store?orders(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه فعال نیست.");/* راهنما: این شرط بررسی می‌کند آیا «b.actionType==="SUPPORT"||b.actionType==="TEXT"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b.actionType==="SUPPORT"||b.actionType==="TEXT")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,b.actionValue||b.title)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,b.actionValue||b.title);/* راهنما: این شرط بررسی می‌کند آیا «b.actionType==="URL"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b.actionType==="URL")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «send(token,chatId,`${b.title}\n${b.actionValue||""}`.trim())» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return send(token,chatId,`${b.title}\n${b.actionValue||""}`.trim());/* راهنما: این شرط بررسی می‌کند آیا «b.actionType==="SUBMENU"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b.actionType==="SUBMENU"){/* راهنما: این دستور متغیر/ثابت «kids» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const kids=children(all,b.id);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «kids.length?send(token,chatId,b.actionValue||b.title,kids.map(x=>({id:x.id…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return kids.length?send(token,chatId,b.actionValue||b.title,kids.map(x=>({id:x.id,title:x.title}))):send(token,chatId,"این زیرمنو هنوز گزینه‌ای ندارد.")}}

// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{fetch:withSupabase({auth:"none"},async(request,ctx)=>{/* راهنما: این شرط بررسی می‌کند آیا «request.method!=="POST"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(request.method!=="POST")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok");/* راهنما: این دستور متغیر/ثابت «parts» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const parts=new URL(request.url).pathname.split("/").filter(Boolean);/* راهنما: این دستور متغیر/ثابت «secret، rubikaBotId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const secret=parts.at(-1)??"",rubikaBotId=parts.at(-2)??"";/* راهنما: این شرط بررسی می‌کند آیا «!secret||!rubikaBotId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!secret||!rubikaBotId)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("unauthorized",{status:401})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("unauthorized",{status:401});/* راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «{data:bot,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:bot,error}=await admin.from("RubikaBot").select("id,workspaceId,tokenCiphertext,welcomeMessage,webhookSecretHash,status").eq("rubikaBotId",decodeURIComponent(rubikaBotId)).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error||!bot||bot.status!=="ACTIVE"||!bot.webhookSecretHash» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error||!bot||bot.status!=="ACTIVE"||!bot.webhookSecretHash)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("not found",{status:404})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("not found",{status:404});/* راهنما: این شرط بررسی می‌کند آیا «await sha256(secret)!==bot.webhookSecretHash» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(await sha256(secret)!==bot.webhookSecretHash)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("unauthorized",{status:401})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("unauthorized",{status:401});/* راهنما: این دستور متغیر/ثابت «update» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let update:Update;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «update=await request.json()». */ update=await request.json()}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")};/* راهنما: این دستور متغیر/ثابت «[{data:key},{data:br}]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [{data:key},{data:br}]=await Promise.all([admin.from("AppSecret").select("value").eq("id","rubika_token_encryption").single(),admin.from("RubikaButton").select("id,parentId,title,actionType,actionValue,sortOrder").eq("botId",bot.id).order("sortOrder")]);/* راهنما: این شرط بررسی می‌کند آیا «!key?.value» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!key?.value)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok");/* راهنما: این دستور متغیر/ثابت «token» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let token:string;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «token=await decrypt(bot.tokenCiphertext,key.value)». */ token=await decrypt(bot.tokenCiphertext,key.value)}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")};/* راهنما: این دستور متغیر/ثابت «buttons، root، chatId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const buttons=(br??[]) as Btn[],root=roots(buttons),chatId=update.chat_id;/* راهنما: این شرط بررسی می‌کند آیا «!chatId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!chatId)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok");/* راهنما: این دستور متغیر/ثابت «msg، senderId، buttonId، text، store» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const msg=update.new_message??update.updated_message,senderId=msg?.sender_id,buttonId=msg?.aux_data?.button_id,text=msg?.text?.trim()??"",store=await storeFor(admin,bot.workspaceId).catch(()=>null);/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این شرط بررسی می‌کند آیا «update.type==="StartedBot"||text==="/start"||text==="start"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(update.type==="StartedBot"||text==="/start"||text==="start"){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await send(token,chatId,bot.welcomeMessage||"سلام! از منوی زیر انتخاب کنید.",root.map(x=>…». */ await send(token,chatId,bot.welcomeMessage||"سلام! از منوی زیر انتخاب کنید.",root.map(x=>({id:x.id,title:x.title})));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")}/* راهنما: این شرط بررسی می‌کند آیا «buttonId&&senderId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId&&senderId){/* راهنما: این شرط بررسی می‌کند آیا «buttonId==="catalog:view"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId==="catalog:view")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?catalog(admin,token,chatId,store):send(token,chatId,"فروشگاه فعال نیست."))». */ await (store?catalog(admin,token,chatId,store):send(token,chatId,"فروشگاه فعال نیست."));else /* راهنما: این شرط بررسی می‌کند آیا «buttonId==="cart:view"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId==="cart:view")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?cart(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه فعال نیست…». */ await (store?cart(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه فعال نیست."));else /* راهنما: این شرط بررسی می‌کند آیا «buttonId==="orders:view"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId==="orders:view")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?orders(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه فعال نی…». */ await (store?orders(admin,token,chatId,store,senderId):send(token,chatId,"فروشگاه فعال نیست."));else /* راهنما: این شرط بررسی می‌کند آیا «buttonId==="checkout"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId==="checkout")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?checkout(admin,token,chatId,store,senderId,msg?.message_id):send(token,chatI…». */ await (store?checkout(admin,token,chatId,store,senderId,msg?.message_id):send(token,chatId,"فروشگاه فعال نیست."));else /* راهنما: این شرط بررسی می‌کند آیا «buttonId.startsWith("add:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId.startsWith("add:"))/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?changeCart(admin,token,chatId,store,senderId,buttonId.slice(4),1):send(token…». */ await (store?changeCart(admin,token,chatId,store,senderId,buttonId.slice(4),1):send(token,chatId,"فروشگاه فعال نیست."));else /* راهنما: این شرط بررسی می‌کند آیا «buttonId.startsWith("inc:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId.startsWith("inc:"))/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?changeCart(admin,token,chatId,store,senderId,buttonId.slice(4),1):send(token…». */ await (store?changeCart(admin,token,chatId,store,senderId,buttonId.slice(4),1):send(token,chatId,"فروشگاه فعال نیست."));else /* راهنما: این شرط بررسی می‌کند آیا «buttonId.startsWith("dec:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(buttonId.startsWith("dec:"))/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await (store?changeCart(admin,token,chatId,store,senderId,buttonId.slice(4),-1):send(toke…». */ await (store?changeCart(admin,token,chatId,store,senderId,buttonId.slice(4),-1):send(token,chatId,"فروشگاه فعال نیست."));else{/* راهنما: این دستور متغیر/ثابت «b» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const b=buttons.find(x=>x.id===buttonId);/* راهنما: این شرط بررسی می‌کند آیا «b» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(b)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await action(admin,token,chatId,store,senderId,msg?.message_id,b,buttons)». */ await action(admin,token,chatId,store,senderId,msg?.message_id,b,buttons);else /* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await send(token,chatId,"یکی از گزینه‌های منو را انتخاب کنید.",root.map(x=>({id:x.id,titl…». */ await send(token,chatId,"یکی از گزینه‌های منو را انتخاب کنید.",root.map(x=>({id:x.id,title:x.title}))) }/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")}/* راهنما: این شرط بررسی می‌کند آیا «text==="/products"&&store» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(text==="/products"&&store)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await catalog(admin,token,chatId,store)». */ await catalog(admin,token,chatId,store);else /* راهنما: این شرط بررسی می‌کند آیا «text==="/orders"&&store&&senderId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(text==="/orders"&&store&&senderId)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await orders(admin,token,chatId,store,senderId)». */ await orders(admin,token,chatId,store,senderId);else /* راهنما: این شرط بررسی می‌کند آیا «text==="/cart"&&store&&senderId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(text==="/cart"&&store&&senderId)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await cart(admin,token,chatId,store,senderId)». */ await cart(admin,token,chatId,store,senderId);else /* راهنما: این شرط بررسی می‌کند آیا «text» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(text)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await send(token,chatId,"یکی از گزینه‌های منو را انتخاب کنید.",root.map(x=>({id:x.id,titl…». */ await send(token,chatId,"یکی از گزینه‌های منو را انتخاب کنید.",root.map(x=>({id:x.id,title:x.title})))}catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("rubika webhook",e)». */ console.error("rubika webhook",e);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await send(token,chatId,"در پردازش درخواست مشکلی پیش آمد.",root.map(x=>({id:x.id,title:x.…». */ await send(token,chatId,"در پردازش درخواست مشکلی پیش آمد.",root.map(x=>({id:x.id,title:x.title}))).catch(()=>undefined)}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")})};
