/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";

// راهنما: این دستور متغیر/ثابت «corsHeaders» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"apikey, content-type, x-forwarded-for","Access-Control-Allow-Methods":"GET, POST, OPTIONS"};
// راهنما: این دستور متغیر/ثابت «json» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const json=(data:unknown,status=200)=>Response.json(data,{status,headers:{...corsHeaders,"Cache-Control":"no-store"}});
// راهنما: این دستور متغیر/ثابت «validSlug» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const validSlug=(v:string)=>/^[a-z0-9][a-z0-9-]{2,63}$/.test(v);
// راهنما: این دستور متغیر/ثابت «validDate» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const validDate=(v:string)=>/^\d{4}-\d{2}-\d{2}$/.test(v);
// راهنما: این دستور متغیر/ثابت «validTime» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const validTime=(v:string)=>/^([01]\d|2[0-3]):[0-5]\d$/.test(v);

// راهنما: این تابع «localToUtc» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function localToUtc(date:string,time:string,timeZone:string){/* راهنما: این دستور متغیر/ثابت «[y,m,d]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const[y,m,d]=date.split("-").map(Number);/* راهنما: این دستور متغیر/ثابت «[h,min]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const[h,min]=time.split(":").map(Number);/* راهنما: این دستور متغیر/ثابت «guess» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const guess=Date.UTC(y,m-1,d,h,min,0);/* راهنما: این دستور متغیر/ثابت «f» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const f=new Intl.DateTimeFormat("en-US",{timeZone,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hourCycle:"h23"});/* راهنما: این دستور متغیر/ثابت «parts» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const parts=(value:number)=>{/* راهنما: این دستور متغیر/ثابت «map» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const map=Object.fromEntries(f.formatToParts(new Date(value)).map(p=>[p.type,p.value]));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «Date.UTC(Number(map.year),Number(map.month)-1,Number(map.day),Number(map.h…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return Date.UTC(Number(map.year),Number(map.month)-1,Number(map.day),Number(map.hour),Number(map.minute),Number(map.second));};/* راهنما: این دستور متغیر/ثابت «offset» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let offset=parts(guess)-guess;/* راهنما: این دستور متغیر/ثابت «result» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let result=guess-offset;/* راهنما: این دستور متغیر/ثابت «adjusted» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const adjusted=parts(result)-result;/* راهنما: این شرط بررسی می‌کند آیا «adjusted!==offset» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(adjusted!==offset)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «result=guess-adjusted». */ result=guess-adjusted;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Date(result)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Date(result);}
// راهنما: این دستور متغیر/ثابت «mins» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const mins=(v:string)=>{/* راهنما: این دستور متغیر/ثابت «[h,m]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const[h,m]=v.slice(0,5).split(":").map(Number);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «h*60+m» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return h*60+m};
// راهنما: این دستور متغیر/ثابت «label» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const label=(total:number)=>`${Math.floor(total/60).toString().padStart(2,"0")}:${(total%60).toString().padStart(2,"0")}`;

// راهنما: این تابع «publicContext» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function publicContext(admin:any,slug:string){
  // راهنما: این دستور متغیر/ثابت «{data:settings,error:settingsError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const{data:settings,error:settingsError}=await admin.from("BookingSettings").select("workspaceId,timezone,slotIntervalMinutes,minBookingNoticeMinutes,cancellationNoticeMinutes,allowCustomerCancellation,requireDeposit,defaultDepositPercent,publicBookingEnabled,publicSlug").eq("publicSlug",slug).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «settingsError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(settingsError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw settingsError;/* راهنما: این شرط بررسی می‌کند آیا «!settings?.publicBookingEnabled» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!settings?.publicBookingEnabled)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «null» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return null;
  // راهنما: این دستور متغیر/ثابت «[workspace,services,staff,assignments]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const[workspace,services,staff,assignments]=await Promise.all([
    admin.from("Workspace").select("id,name").eq("id",settings.workspaceId).maybeSingle(),
    admin.from("BookingService").select("id,title,description,durationMinutes,bufferMinutes,priceAmount,currency,depositPercent,color").eq("workspaceId",settings.workspaceId).eq("isActive",true).order("createdAt",{ascending:true}),
    admin.from("BookingStaff").select("id,name,roleTitle,color").eq("workspaceId",settings.workspaceId).eq("isActive",true).order("createdAt",{ascending:true}),
    admin.from("BookingStaffService").select("staffId,serviceId,priceAmountOverride,durationMinutesOverride,BookingStaff!inner(workspaceId)").eq("BookingStaff.workspaceId",settings.workspaceId),
  ]);/* راهنما: این دستور متغیر/ثابت «error» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const error=[workspace.error,services.error,staff.error,assignments.error].find(Boolean);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;
  // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «{settings,workspace:workspace.data,services:services.data??[],staff:staff.…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
  return{settings,workspace:workspace.data,services:services.data??[],staff:staff.data??[],assignments:(assignments.data??[]).map((x:any)=>({staffId:x.staffId,serviceId:x.serviceId,priceAmountOverride:x.priceAmountOverride,durationMinutesOverride:x.durationMinutesOverride}))};
}

// راهنما: این تابع «slotsFor» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function slotsFor(admin:any,ctx:any,date:string,serviceId:string,staffId:string){
  // راهنما: این دستور متغیر/ثابت «service» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const service=ctx.services.find((x:any)=>x.id===serviceId);/* راهنما: این دستور متغیر/ثابت «staff» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const staff=ctx.staff.find((x:any)=>x.id===staffId);/* راهنما: این دستور متغیر/ثابت «assignment» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const assignment=ctx.assignments.find((x:any)=>x.staffId===staffId&&x.serviceId===serviceId);/* راهنما: این شرط بررسی می‌کند آیا «!service||!staff||!assignment||!validDate(date)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!service||!staff||!assignment||!validDate(date))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «[]» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return[];
  // راهنما: این دستور متغیر/ثابت «weekday» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const weekday=new Date(`${date}T00:00:00Z`).getUTCDay();/* راهنما: این دستور متغیر/ثابت «{data:hour,error:hourError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:hour,error:hourError}=await admin.from("BookingWorkingHour").select("startTime,endTime,isWorking").eq("staffId",staffId).eq("weekday",weekday).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «hourError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(hourError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw hourError;/* راهنما: این شرط بررسی می‌کند آیا «!hour?.isWorking» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!hour?.isWorking)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «[]» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return[];
  // راهنما: این دستور متغیر/ثابت «startTime، endTime، timezone» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const startTime=hour.startTime.slice(0,5),endTime=hour.endTime.slice(0,5),timezone=ctx.settings.timezone||"Asia/Tehran";/* راهنما: این دستور متغیر/ثابت «dayStart» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const dayStart=localToUtc(date,"00:00",timezone);/* راهنما: این دستور متغیر/ثابت «next» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const next=new Date(dayStart.getTime()+36*3600000);/* راهنما: این دستور متغیر/ثابت «nextDate» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const nextDate=new Intl.DateTimeFormat("en-CA",{timeZone:timezone,year:"numeric",month:"2-digit",day:"2-digit"}).format(next);/* راهنما: این دستور متغیر/ثابت «dayEnd» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const dayEnd=localToUtc(nextDate,"00:00",timezone);
  // راهنما: این دستور متغیر/ثابت «[appointments,timeOff]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const[appointments,timeOff]=await Promise.all([admin.from("BookingAppointment").select("startsAt,endsAt").eq("workspaceId",ctx.settings.workspaceId).eq("staffId",staffId).in("status",["PENDING","CONFIRMED"]).gte("startsAt",dayStart.toISOString()).lt("startsAt",dayEnd.toISOString()),admin.from("BookingTimeOff").select("startsAt,endsAt").eq("workspaceId",ctx.settings.workspaceId).eq("staffId",staffId).lt("startsAt",dayEnd.toISOString()).gt("endsAt",dayStart.toISOString())]);/* راهنما: این دستور متغیر/ثابت «error» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const error=[appointments.error,timeOff.error].find(Boolean);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;
  // راهنما: این دستور متغیر/ثابت «duration» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const duration=Number(assignment.durationMinutesOverride??service.durationMinutes)+Number(service.bufferMinutes||0);/* راهنما: این دستور متغیر/ثابت «interval» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const interval=Math.max(5,Number(ctx.settings.slotIntervalMinutes||15));/* راهنما: این دستور متغیر/ثابت «nowLimit» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const nowLimit=Date.now()+Number(ctx.settings.minBookingNoticeMinutes||0)*60000;/* راهنما: این دستور متغیر/ثابت «start، end» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const start=mins(startTime),end=mins(endTime);/* راهنما: این دستور متغیر/ثابت «result» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const result:any[]=[];
  // راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند.
  for(let t=start;t+duration<=end;t+=interval){/* راهنما: این دستور متغیر/ثابت «time» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const time=label(t);/* راهنما: این دستور متغیر/ثابت «startsAt» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const startsAt=localToUtc(date,time,timezone);/* راهنما: این دستور متغیر/ثابت «endsAt» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const endsAt=new Date(startsAt.getTime()+duration*60000);/* راهنما: این شرط بررسی می‌کند آیا «startsAt.getTime()<nowLimit» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(startsAt.getTime()<nowLimit)/* راهنما: این دستور ادامه دستورات مرحله فعلی حلقه را رد می‌کند و به تکرار بعدی می‌رود. */ continue;/* راهنما: این دستور متغیر/ثابت «conflict» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const conflict=[...(appointments.data??[]),...(timeOff.data??[])].some((a:any)=>new Date(a.startsAt).getTime()<endsAt.getTime()&&new Date(a.endsAt).getTime()>startsAt.getTime());/* راهنما: این شرط بررسی می‌کند آیا «!conflict» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!conflict)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «result.push({time,startsAt:startsAt.toISOString()})». */ result.push({time,startsAt:startsAt.toISOString()});}
  // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «result» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
  return result;
}

// راهنما: این دستور متغیر/ثابت «handler» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const handler=withSupabase({auth:"none"},async(request,ctx)=>{
  // راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «url» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const url=new URL(request.url);
  // راهنما: این شرط بررسی می‌کند آیا «request.method==="GET"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(request.method==="GET"){
    // راهنما: این دستور متغیر/ثابت «slug» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
    const slug=(url.searchParams.get("slug")??"").trim().toLowerCase();/* راهنما: این شرط بررسی می‌کند آیا «!validSlug(slug)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!validSlug(slug))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"لینک رزرو معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"لینک رزرو معتبر نیست."},400);
    // راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود.
    try{/* راهنما: این دستور متغیر/ثابت «info» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const info=await publicContext(admin,slug);/* راهنما: این شرط بررسی می‌کند آیا «!info» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!info)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"صفحه رزرو پیدا نشد یا غیرفعال است."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"صفحه رزرو پیدا نشد یا غیرفعال است."},404);/* راهنما: این دستور متغیر/ثابت «date، serviceId، staffId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const date=url.searchParams.get("date")??"",serviceId=url.searchParams.get("serviceId")??"",staffId=url.searchParams.get("staffId")??"";/* راهنما: این دستور متغیر/ثابت «slots» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const slots=date&&serviceId&&staffId?await slotsFor(admin,info,date,serviceId,staffId):[];/* راهنما: این دستور متغیر/ثابت «availableStaff» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const availableStaff=serviceId?info.staff.filter((s:any)=>info.assignments.some((a:any)=>a.staffId===s.id&&a.serviceId===serviceId)):info.staff;/* راهنما: این دستور متغیر/ثابت «services» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const services=info.services.map((s:any)=>({...s}));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true,business:{name:info.workspace?.name??"کسب‌وکار",slug},servic…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true,business:{name:info.workspace?.name??"کسب‌وکار",slug},services,staff:availableStaff,staffServices:info.assignments,booking:{timezone:info.settings.timezone,minBookingNoticeMinutes:info.settings.minBookingNoticeMinutes,requireDeposit:info.settings.requireDeposit,defaultDepositPercent:info.settings.defaultDepositPercent},slots});}catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("booking-public-get",e)». */ console.error("booking-public-get",e);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات رزرو در دسترس نیست."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات رزرو در دسترس نیست."},500)}
  }
  // راهنما: این شرط بررسی می‌کند آیا «request.method!=="POST"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(request.method!=="POST")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Method not allowed"},405)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Method not allowed"},405);/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let body:any;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body=await request.json()». */ body=await request.json()}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"درخواست معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"درخواست معتبر نیست."},400)}/* راهنما: این شرط بررسی می‌کند آیا «body?.website» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(body?.website)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true,ignored:true},202)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true,ignored:true},202);
  // راهنما: این دستور متغیر/ثابت «slug، customerName، phone، serviceId، staffId، date، time، note» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
  const slug=typeof body.slug==="string"?body.slug.trim().toLowerCase():"",customerName=typeof body.customerName==="string"?body.customerName.trim().replace(/\s+/g," ").slice(0,140):"",phone=typeof body.phone==="string"?body.phone.trim().replace(/\s+/g,"").slice(0,30):"",serviceId=typeof body.serviceId==="string"?body.serviceId:"",staffId=typeof body.staffId==="string"?body.staffId:"",date=typeof body.date==="string"?body.date:"",time=typeof body.time==="string"?body.time:"",note=typeof body.note==="string"?body.note.trim().slice(0,1000)||null:null;/* راهنما: این شرط بررسی می‌کند آیا «!validSlug(slug)||customerName.length<2||!/^\+?[0-9]{8,18}$/.test(phone)||!serv…» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!validSlug(slug)||customerName.length<2||!/^\+?[0-9]{8,18}$/.test(phone)||!serviceId||!staffId||!validDate(date)||!validTime(time))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات رزرو ناقص یا نامعتبر است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات رزرو ناقص یا نامعتبر است."},400);
  // راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود.
  try{/* راهنما: این دستور متغیر/ثابت «info» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const info=await publicContext(admin,slug);/* راهنما: این شرط بررسی می‌کند آیا «!info» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!info)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"رزرو آنلاین این کسب‌وکار فعال نیست."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"رزرو آنلاین این کسب‌وکار فعال نیست."},404);/* راهنما: این دستور متغیر/ثابت «service، staff، assignment» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const service=info.services.find((x:any)=>x.id===serviceId),staff=info.staff.find((x:any)=>x.id===staffId),assignment=info.assignments.find((x:any)=>x.staffId===staffId&&x.serviceId===serviceId);/* راهنما: این شرط بررسی می‌کند آیا «!service||!staff||!assignment» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!service||!staff||!assignment)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این خدمت توسط این پرسنل ارائه نمی‌شود."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این خدمت توسط این پرسنل ارائه نمی‌شود."},400);/* راهنما: این دستور متغیر/ثابت «allowed» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const allowed=await slotsFor(admin,info,date,serviceId,staffId);/* راهنما: این دستور متغیر/ثابت «selected» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const selected=allowed.find((x:any)=>x.time===time);/* راهنما: این شرط بررسی می‌کند آیا «!selected» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!selected)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این ساعت دیگر در دسترس نیست. یک زمان دیگر انتخاب ک…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این ساعت دیگر در دسترس نیست. یک زمان دیگر انتخاب کنید."},409);
    // راهنما: این دستور متغیر/ثابت «{data:existing,error:existingError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
    const{data:existing,error:existingError}=await admin.from("BookingCustomer").select("id").eq("workspaceId",info.settings.workspaceId).eq("phone",phone).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «existingError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(existingError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw existingError;/* راهنما: این شرط بررسی می‌کند آیا «existing?.id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(existing?.id){/* راهنما: این دستور متغیر/ثابت «oneHourAgo» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const oneHourAgo=new Date(Date.now()-3600000).toISOString();/* راهنما: این دستور متغیر/ثابت «{count,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{count,error}=await admin.from("BookingAppointment").select("id",{count:"exact",head:true}).eq("workspaceId",info.settings.workspaceId).eq("customerId",existing.id).eq("source","PUBLIC_BOOKING").gte("createdAt",oneHourAgo);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این شرط بررسی می‌کند آیا «(count??0)>=5» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if((count??0)>=5)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"تعداد تلاش‌های رزرو زیاد است. کمی بعد دوباره امتحا…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"تعداد تلاش‌های رزرو زیاد است. کمی بعد دوباره امتحان کنید."},429)}
    // راهنما: این دستور متغیر/ثابت «{data:customer,error:customerError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
    const{data:customer,error:customerError}=await admin.from("BookingCustomer").upsert({workspaceId:info.settings.workspaceId,fullName:customerName,phone},{onConflict:"workspaceId,phone"}).select("id").single();/* راهنما: این شرط بررسی می‌کند آیا «customerError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(customerError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw customerError;/* راهنما: این دستور متغیر/ثابت «duration، amount» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const duration=Number(assignment.durationMinutesOverride??service.durationMinutes)+Number(service.bufferMinutes||0),amount=Number(assignment.priceAmountOverride??service.priceAmount);/* راهنما: این دستور متغیر/ثابت «startsAt، endsAt» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const startsAt=new Date(selected.startsAt),endsAt=new Date(startsAt.getTime()+duration*60000);/* راهنما: این دستور متغیر/ثابت «depositPercent، depositAmount، status» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const depositPercent=service.depositPercent??info.settings.defaultDepositPercent??0,depositAmount=info.settings.requireDeposit?Math.round(amount*Number(depositPercent)/100):0,status=info.settings.requireDeposit?"PENDING":"CONFIRMED";/* راهنما: این دستور متغیر/ثابت «{data:appointment,error:appointmentError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:appointment,error:appointmentError}=await admin.from("BookingAppointment").insert({workspaceId:info.settings.workspaceId,customerId:customer.id,serviceId,staffId,startsAt:startsAt.toISOString(),endsAt:endsAt.toISOString(),status,amount,depositAmount,currency:service.currency??"IRR",source:"PUBLIC_BOOKING",note}).select("id,status,startsAt,amount,depositAmount,currency").single();/* راهنما: این شرط بررسی می‌کند آیا «appointmentError?.code==="23P01"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(appointmentError?.code==="23P01")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این ساعت همین الان رزرو شد. زمان دیگری انتخاب کنید…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این ساعت همین الان رزرو شد. زمان دیگری انتخاب کنید."},409);/* راهنما: این شرط بررسی می‌کند آیا «appointmentError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(appointmentError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw appointmentError;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true,appointment,business:{name:info.workspace?.name??"کسب‌وکار"}…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true,appointment,business:{name:info.workspace?.name??"کسب‌وکار"},service:{title:service.title},staff:{name:staff.name},message:status==="CONFIRMED"?"نوبت با موفقیت ثبت شد.":"نوبت ثبت شد و منتظر تأیید است."},201);
  }catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("booking-public-post",e)». */ console.error("booking-public-post",e);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ثبت نوبت انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ثبت نوبت انجام نشد."},500)}
});

// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{async fetch(request:Request){/* راهنما: این شرط بررسی می‌کند آیا «request.method==="OPTIONS"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(request.method==="OPTIONS")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok",{headers:corsHeaders})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok",{headers:corsHeaders});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «handler(request)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return handler(request)}};
