/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";

// راهنما: این Type با نام «User» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type User={id:number;username?:string;first_name?:string;last_name?:string};
// راهنما: این Type با نام «Msg» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Msg={message_id?:number;chat?:{id?:number};from?:User;text?:string};
// راهنما: این Type با نام «Update» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Update={message?:Msg;callback_query?:{id?:string;from?:User;data?:string;message?:Msg}};
// راهنما: این Type با نام «Btn» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Btn={id:string;parentId?:string|null;title:string;actionType:string;actionValue?:string|null;sortOrder:number};

// راهنما: این دستور متغیر/ثابت «hex» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const hex=(b:Uint8Array)=>Array.from(b,x=>x.toString(16).padStart(2,"0")).join("");
// راهنما: این تابع «sha256» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function sha256(v:string){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «hex(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return hex(new Uint8Array(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v))))}
// راهنما: این تابع «fromHex» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function fromHex(v:string){/* راهنما: این شرط بررسی می‌کند آیا «!/^[0-9a-f]{64}$/i.test(v)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!/^[0-9a-f]{64}$/i.test(v))/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error("bad_key");/* راهنما: این دستور متغیر/ثابت «b» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const b=new Uint8Array(v.length/2);/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(let i=0;i<b.length;i++)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «b[i]=parseInt(v.slice(i*2,i*2+2),16)». */ b[i]=parseInt(v.slice(i*2,i*2+2),16);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «b» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return b}
// راهنما: این تابع «from64» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function from64(v:string){/* راهنما: این دستور متغیر/ثابت «s، b» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const s=atob(v),b=new Uint8Array(s.length);/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(let i=0;i<s.length;i++)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «b[i]=s.charCodeAt(i)». */ b[i]=s.charCodeAt(i);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «b» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return b}
// راهنما: این تابع «decrypt» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function decrypt(c:string,k:string){/* راهنما: این دستور متغیر/ثابت «[v,iv,p]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [v,iv,p]=c.split(":");/* راهنما: این شرط بررسی می‌کند آیا «v!=="v1"||!iv||!p» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(v!=="v1"||!iv||!p)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error("bad_cipher");/* راهنما: این دستور متغیر/ثابت «key» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const key=await crypto.subtle.importKey("raw",fromHex(k),{name:"AES-GCM"},false,["decrypt"]);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new TextDecoder().decode(await crypto.subtle.decrypt({name:"AES-GCM",iv:fr…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new TextDecoder().decode(await crypto.subtle.decrypt({name:"AES-GCM",iv:from64(iv)},key,from64(p)))}
// راهنما: این دستور متغیر/ثابت «money» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const money=(v:any,c="IRR")=>`${new Intl.NumberFormat("fa-IR").format(Number(v??0))} ${c==="IRR"?"ریال":c}`;
// راهنما: این دستور متغیر/ثابت «nameOf» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const nameOf=(u?:User)=>u?[u.first_name,u.last_name].filter(Boolean).join(" ").trim():"";

// راهنما: این تابع «tg» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function tg(token:string,method:string,body:Record<string,unknown>){/* راهنما: این متغیر «r» نتیجه یا Promise یک درخواست شبکه را نگه می‌دارد. */ const r=await fetch(`https://api.telegram.org/bot${token}/${method}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});/* راهنما: این شرط بررسی می‌کند آیا «!r.ok» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!r.ok)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error(method,r.status)». */ console.error(method,r.status);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «r» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return r}
// راهنما: این تابع «sendInline» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function sendInline(token:string,chatId:number,text:string,rows:any[][]){/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const body:Record<string,unknown>={chat_id:chatId,text};/* راهنما: این شرط بررسی می‌کند آیا «rows.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(rows.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body.reply_markup={inline_keyboard:rows}». */ body.reply_markup={inline_keyboard:rows};/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «tg(token,"sendMessage",body)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return tg(token,"sendMessage",body)}
// راهنما: این تابع «sendMenu» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function sendMenu(token:string,chatId:number,text:string,buttons:Btn[]){/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=[];/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(let i=0;i<buttons.length;i+=2)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «rows.push(buttons.slice(i,i+2).map(b=>({text:b.title})))». */ rows.push(buttons.slice(i,i+2).map(b=>({text:b.title})));/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const body:Record<string,unknown>={chat_id:chatId,text};/* راهنما: این شرط بررسی می‌کند آیا «rows.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(rows.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body.reply_markup={keyboard:rows,resize_keyboard:true,is_persistent:true}». */ body.reply_markup={keyboard:rows,resize_keyboard:true,is_persistent:true};/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «tg(token,"sendMessage",body)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return tg(token,"sendMessage",body)}
// راهنما: این تابع «ack» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function ack(token:string,id?:string,text?:string){/* راهنما: این شرط بررسی می‌کند آیا «id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(id)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await tg(token,"answerCallbackQuery",{callback_query_id:id,...(text?{text}:{})})». */ await tg(token,"answerCallbackQuery",{callback_query_id:id,...(text?{text}:{})})}

// راهنما: این تابع «storeFor» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function storeFor(admin:any,workspaceId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.from("Store").select("id,name,currency,status").eq("workspaceId",workspaceId).eq("status","ACTIVE").maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data}
// راهنما: این تابع «catalog» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function catalog(admin:any,token:string,chatId:number,store:any){/* راهنما: این دستور متغیر/ثابت «[{data:cats,error:ce},{data:items,error:ie}]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [{data:cats,error:ce},{data:items,error:ie}]=await Promise.all([admin.from("StoreCategory").select("id,title").eq("storeId",store.id).eq("isActive",true).order("sortOrder").limit(10),admin.from("StoreItem").select("id,title,priceAmount,currency").eq("storeId",store.id).eq("isActive",true).order("sortOrder").limit(12)]);/* راهنما: این شرط بررسی می‌کند آیا «ce||ie» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(ce||ie)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw ce||ie;/* راهنما: این شرط بررسی می‌کند آیا «!(items??[]).length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!(items??[]).length)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,`فروشگاه «${store.name}» هنوز محصول فعالی ندارد.`,…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,`فروشگاه «${store.name}» هنوز محصول فعالی ندارد.`,[]);/* راهنما: این شرط بررسی می‌کند آیا «(cats??[]).length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if((cats??[]).length){/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=(cats??[]).map((c:any)=>[{text:c.title,callback_data:`cat:${c.id}`}]);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «rows.push([{text:"همه محصولات",callback_data:"cat:all"}])». */ rows.push([{text:"همه محصولات",callback_data:"cat:all"}]);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,`🛍 محصولات «${store.name}»`,rows)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,`🛍 محصولات «${store.name}»`,rows)}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,`🛍 محصولات «${store.name}»`,(items??[]).map((i:an…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,`🛍 محصولات «${store.name}»`,(items??[]).map((i:any)=>[{text:`${i.title} — ${money(i.priceAmount,i.currency)}`,callback_data:`product:${i.id}`}]))}
// راهنما: این تابع «category» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function category(admin:any,token:string,chatId:number,store:any,id:string){/* راهنما: این دستور متغیر/ثابت «q» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let q=admin.from("StoreItem").select("id,title,priceAmount,currency").eq("storeId",store.id).eq("isActive",true).order("sortOrder").limit(12);/* راهنما: این شرط بررسی می‌کند آیا «id!=="all"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(id!=="all")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «q=q.eq("categoryId",id)». */ q=q.eq("categoryId",id);/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await q;/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=(data??[]).map((i:any)=>[{text:`${i.title} — ${money(i.priceAmount,i.currency)}`,callback_data:`product:${i.id}`}]);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «rows.push([{text:"← بازگشت",callback_data:"catalog"}])». */ rows.push([{text:"← بازگشت",callback_data:"catalog"}]);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,rows.length===1?"در این دسته محصولی نیست.":"محصول …» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,rows.length===1?"در این دسته محصولی نیست.":"محصول را انتخاب کنید:",rows)}
// راهنما: این تابع «product» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function product(admin:any,token:string,chatId:number,store:any,id:string){/* راهنما: این دستور متغیر/ثابت «{data:i,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:i,error}=await admin.from("StoreItem").select("id,title,description,priceAmount,currency,inventoryCount").eq("id",id).eq("storeId",store.id).eq("isActive",true).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این شرط بررسی می‌کند آیا «!i» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!i)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"این محصول دیگر در دسترس نیست.",[[{text:"← محصولات…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"این محصول دیگر در دسترس نیست.",[[{text:"← محصولات",callback_data:"catalog"}]]);/* راهنما: این دستور متغیر/ثابت «sold» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const sold=i.inventoryCount!==null&&Number(i.inventoryCount)<=0;/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows:any[][]=[];/* راهنما: این شرط بررسی می‌کند آیا «!sold» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!sold)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «rows.push([{text:"➕ افزودن به سبد",callback_data:`cartadd:${i.id}`}])». */ rows.push([{text:"➕ افزودن به سبد",callback_data:`cartadd:${i.id}`}]);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «rows.push([{text:"← محصولات",callback_data:"catalog"}])». */ rows.push([{text:"← محصولات",callback_data:"catalog"}]);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,`📦 ${i.title}\n💳 ${money(i.priceAmount,i.currenc…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,`📦 ${i.title}\n💳 ${money(i.priceAmount,i.currency)}\n${i.inventoryCount===null?"موجودی: نامحدود":`موجودی: ${i.inventoryCount}`}\n${i.description??""}${sold?"\n⛔️ ناموجود":""}`,rows)}
// راهنما: این تابع «cartSnapshot» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function cartSnapshot(admin:any,storeId:string,userId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.rpc("telegram_cart_snapshot",{p_store_id:storeId,p_external_user_id:userId});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data}
// راهنما: این تابع «showCart» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function showCart(admin:any,token:string,chatId:number,store:any,user?:User){/* راهنما: این شرط بررسی می‌کند آیا «!user?.id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!user?.id)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"کاربر قابل شناسایی نیست.",[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"کاربر قابل شناسایی نیست.",[]);/* راهنما: این دستور متغیر/ثابت «c» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const c=await cartSnapshot(admin,store.id,String(user.id));/* راهنما: این شرط بررسی می‌کند آیا «!c?.itemCount» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!c?.itemCount)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"🛒 سبد خرید شما خالی است.",[[{text:"🛍 مشاهده محص…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"🛒 سبد خرید شما خالی است.",[[{text:"🛍 مشاهده محصولات",callback_data:"catalog"}]]);/* راهنما: این دستور متغیر/ثابت «lines» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const lines=["🛒 سبد خرید",...(c.items??[]).map((x:any)=>`${x.title} × ${x.quantity} — ${money(x.lineTotalAmount,x.currency)}`),`\nجمع: ${money(c.totalAmount,c.currency)}`];/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=(c.items??[]).flatMap((x:any)=>[[{text:`− ${x.title}`,callback_data:`cartdel:${x.itemId}`},{text:`+ ${x.title}`,callback_data:`cartadd:${x.itemId}`}]]);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «rows.push([{text:"ثبت سفارش",callback_data:"checkout"}],[{text:"🛍 ادامه خرید",callback_d…». */ rows.push([{text:"ثبت سفارش",callback_data:"checkout"}],[{text:"🛍 ادامه خرید",callback_data:"catalog"}]);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,lines.join("\n"),rows)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,lines.join("\n"),rows)}
// راهنما: این تابع «changeCart» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function changeCart(admin:any,store:any,user:User|undefined,itemId:string,delta:number){/* راهنما: این شرط بررسی می‌کند آیا «!user?.id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!user?.id)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error("user_missing");/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.rpc("telegram_cart_change",{p_store_id:store.id,p_item_id:itemId,p_external_user_id:String(user.id),p_username:user.username??"",p_display_name:nameOf(user),p_delta:delta});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data}
// راهنما: این تابع «checkout» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function checkout(admin:any,store:any,user:User|undefined,chatId:number,messageId?:number){/* راهنما: این شرط بررسی می‌کند آیا «!user?.id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!user?.id)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error("user_missing");/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.rpc("telegram_checkout_cart",{p_store_id:store.id,p_external_user_id:String(user.id),p_external_conversation_id:String(chatId),p_idempotency_key:`telegram:checkout:${chatId}:${messageId??"no-message"}`});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data}
// راهنما: این تابع «orders» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function orders(admin:any,token:string,chatId:number,store:any,user?:User){/* راهنما: این شرط بررسی می‌کند آیا «!user?.id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!user?.id)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"کاربر قابل شناسایی نیست.",[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"کاربر قابل شناسایی نیست.",[]);/* راهنما: این دستور متغیر/ثابت «{data:c}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:c}=await admin.from("StoreCustomer").select("id").eq("storeId",store.id).eq("platform","telegram").eq("externalUserId",String(user.id)).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «!c» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!c)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"هنوز سفارشی ندارید.",[[{text:"🛍 محصولات",callbac…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"هنوز سفارشی ندارید.",[[{text:"🛍 محصولات",callback_data:"catalog"}]]);/* راهنما: این دستور متغیر/ثابت «{data:o,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:o,error}=await admin.from("StoreOrder").select("id,status,totalAmount,currency").eq("storeId",store.id).eq("customerId",c.id).order("createdAt",{ascending:false}).limit(5);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,(o??[]).length?["📦 سفارش‌های شما",...(o??[]).map(…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,(o??[]).length?["📦 سفارش‌های شما",...(o??[]).map((x:any)=>`#${x.id.slice(0,8)} — ${x.status} — ${money(x.totalAmount,x.currency)}`)].join("\n"):"هنوز سفارشی ندارید.",[[{text:"🛍 محصولات",callback_data:"catalog"}]])}

// راهنما: این تابع «childrenOf» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function childrenOf(buttons:Btn[],parentId:string){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «buttons.filter(b=>b.parentId===parentId).sort((a,b)=>a.sortOrder-b.sortOrd…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return buttons.filter(b=>b.parentId===parentId).sort((a,b)=>a.sortOrder-b.sortOrder)}
// راهنما: این تابع «runMenuAction» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function runMenuAction(admin:any,token:string,chatId:number,store:any,user:User|undefined,button:Btn,buttons:Btn[]){
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="CATALOG"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="CATALOG"){/* راهنما: این شرط بررسی می‌کند آیا «store» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(store)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «catalog(admin,token,chatId,store)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return catalog(admin,token,chatId,store);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"فروشگاه فعال نیست.",[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"فروشگاه فعال نیست.",[])}
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="CART"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="CART"){/* راهنما: این شرط بررسی می‌کند آیا «store» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(store)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «showCart(admin,token,chatId,store,user)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return showCart(admin,token,chatId,store,user);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"فروشگاه فعال نیست.",[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"فروشگاه فعال نیست.",[])}
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="ORDERS"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="ORDERS"){/* راهنما: این شرط بررسی می‌کند آیا «store» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(store)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «orders(admin,token,chatId,store,user)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return orders(admin,token,chatId,store,user);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"فروشگاه فعال نیست.",[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"فروشگاه فعال نیست.",[])}
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="SUPPORT"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="SUPPORT") /* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,button.actionValue||"اطلاعات پشتیبانی هنوز تنظیم ن…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,button.actionValue||"اطلاعات پشتیبانی هنوز تنظیم نشده است.",[]);
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="TEXT"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="TEXT") /* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,button.actionValue||button.title,[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,button.actionValue||button.title,[]);
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="URL"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="URL"){/* راهنما: این دستور متغیر/ثابت «url» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const url=button.actionValue??"";/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,button.title,[[{text:"باز کردن لینک",url}]])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,button.title,[[{text:"باز کردن لینک",url}]])}
  // راهنما: این شرط بررسی می‌کند آیا «button.actionType==="SUBMENU"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
  if(button.actionType==="SUBMENU"){
    // راهنما: این دستور متغیر/ثابت «kids» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
    const kids=childrenOf(buttons,button.id);
    // راهنما: این شرط بررسی می‌کند آیا «!kids.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
    if(!kids.length)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,"این زیرمنو هنوز گزینه‌ای ندارد.",[])» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return sendInline(token,chatId,"این زیرمنو هنوز گزینه‌ای ندارد.",[]);
    // راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
    const rows=kids.map(k=>[{text:k.title,callback_data:`menu:${k.id}`}]);
    // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,button.actionValue||button.title,rows)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
    return sendInline(token,chatId,button.actionValue||button.title,rows);
  }
  // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «sendInline(token,chatId,button.actionValue||`گزینه «${button.title}» انتخا…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
  return sendInline(token,chatId,button.actionValue||`گزینه «${button.title}» انتخاب شد.`,[]);
}

// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{fetch:withSupabase({auth:"none"},async(request,ctx)=>{/* راهنما: این شرط بررسی می‌کند آیا «request.method!=="POST"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(request.method!=="POST")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok");/* راهنما: این دستور متغیر/ثابت «url، telegramBotId، incoming» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const url=new URL(request.url),telegramBotId=url.pathname.split("/").filter(Boolean).pop(),incoming=request.headers.get("x-telegram-bot-api-secret-token")??"";/* راهنما: این شرط بررسی می‌کند آیا «!telegramBotId||!incoming» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!telegramBotId||!incoming)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("unauthorized",{status:401})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("unauthorized",{status:401});/* راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «{data:bot}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:bot}=await admin.from("TelegramBot").select("id,workspaceId,tokenCiphertext,welcomeMessage,webhookSecretHash,status").eq("telegramBotId",telegramBotId).single();/* راهنما: این شرط بررسی می‌کند آیا «!bot||bot.status!=="ACTIVE"||!bot.webhookSecretHash» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!bot||bot.status!=="ACTIVE"||!bot.webhookSecretHash)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("not found",{status:404})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("not found",{status:404});/* راهنما: این شرط بررسی می‌کند آیا «await sha256(incoming)!==bot.webhookSecretHash» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(await sha256(incoming)!==bot.webhookSecretHash)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("unauthorized",{status:401})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("unauthorized",{status:401});/* راهنما: این دستور متغیر/ثابت «u» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let u:Update;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «u=await request.json()». */ u=await request.json()}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")};/* راهنما: این دستور متغیر/ثابت «[{data:s},{data:b}]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [{data:s},{data:b}]=await Promise.all([admin.from("AppSecret").select("value").eq("id","telegram_token_encryption").single(),admin.from("TelegramButton").select("id,parentId,title,actionType,actionValue,sortOrder").eq("botId",bot.id).order("sortOrder")]);/* راهنما: این شرط بررسی می‌کند آیا «!s?.value» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!s?.value)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok");/* راهنما: این دستور متغیر/ثابت «token» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let token:string;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «token=await decrypt(bot.tokenCiphertext,s.value)». */ token=await decrypt(bot.tokenCiphertext,s.value)}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")};/* راهنما: این دستور متغیر/ثابت «buttons، roots، store، cb» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const buttons=(b??[]) as Btn[],roots=buttons.filter(x=>!x.parentId),store=await storeFor(admin,bot.workspaceId).catch(()=>null),cb=u.callback_query;/* راهنما: این شرط بررسی می‌کند آیا «cb?.data&&cb.message?.chat?.id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(cb?.data&&cb.message?.chat?.id){/* راهنما: این دستور متغیر/ثابت «chatId، data» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const chatId=cb.message.chat.id,data=cb.data;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این شرط بررسی می‌کند آیا «data.startsWith("menu:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data.startsWith("menu:")){/* راهنما: این دستور متغیر/ثابت «button» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const button=buttons.find(x=>x.id===data.slice(5));/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id);/* راهنما: این شرط بررسی می‌کند آیا «button» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(button)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await runMenuAction(admin,token,chatId,store,cb.from,button,buttons)». */ await runMenuAction(admin,token,chatId,store,cb.from,button,buttons);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")}/* راهنما: این شرط بررسی می‌کند آیا «!store» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!store){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id,"فروشگاه فعال نیست.")». */ await ack(token,cb.id,"فروشگاه فعال نیست.");/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")}/* راهنما: این شرط بررسی می‌کند آیا «data==="catalog"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data==="catalog"){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await catalog(admin,token,chatId,store)». */ await catalog(admin,token,chatId,store)}else /* راهنما: این شرط بررسی می‌کند آیا «data.startsWith("cat:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data.startsWith("cat:")){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await category(admin,token,chatId,store,data.slice(4))». */ await category(admin,token,chatId,store,data.slice(4))}else /* راهنما: این شرط بررسی می‌کند آیا «data.startsWith("product:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data.startsWith("product:")){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await product(admin,token,chatId,store,data.slice(8))». */ await product(admin,token,chatId,store,data.slice(8))}else /* راهنما: این شرط بررسی می‌کند آیا «data.startsWith("cartadd:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data.startsWith("cartadd:")){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await changeCart(admin,store,cb.from,data.slice(8),1)». */ await changeCart(admin,store,cb.from,data.slice(8),1);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id,"به سبد اضافه شد.")». */ await ack(token,cb.id,"به سبد اضافه شد.");/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await showCart(admin,token,chatId,store,cb.from)». */ await showCart(admin,token,chatId,store,cb.from)}else /* راهنما: این شرط بررسی می‌کند آیا «data.startsWith("cartdel:")» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data.startsWith("cartdel:")){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await changeCart(admin,store,cb.from,data.slice(8),-1)». */ await changeCart(admin,store,cb.from,data.slice(8),-1);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id,"سبد به‌روزرسانی شد.")». */ await ack(token,cb.id,"سبد به‌روزرسانی شد.");/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await showCart(admin,token,chatId,store,cb.from)». */ await showCart(admin,token,chatId,store,cb.from)}else /* راهنما: این شرط بررسی می‌کند آیا «data==="cart"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data==="cart"){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await showCart(admin,token,chatId,store,cb.from)». */ await showCart(admin,token,chatId,store,cb.from)}else /* راهنما: این شرط بررسی می‌کند آیا «data==="checkout"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data==="checkout"){/* راهنما: این دستور متغیر/ثابت «o» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const o=await checkout(admin,store,cb.from,chatId,cb.message.message_id);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id,o.replayed?"این سفارش قبلاً ثبت شده است.":"سفارش ثبت شد.")». */ await ack(token,cb.id,o.replayed?"این سفارش قبلاً ثبت شده است.":"سفارش ثبت شد.");/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await sendInline(token,chatId,`✅ سفارش #${String(o.orderId).slice(0,8)}\nمبلغ: ${money(o.…». */ await sendInline(token,chatId,`✅ سفارش #${String(o.orderId).slice(0,8)}\nمبلغ: ${money(o.totalAmount,o.currency)}\nوضعیت: در انتظار پرداخت\n\nدرگاه پرداخت هنوز فعال نشده است؛ وضعیت سفارش خودکار PAID نمی‌شود.`,[[{text:"📦 سفارش‌های من",callback_data:"orders"}]])}else /* راهنما: این شرط بررسی می‌کند آیا «data==="orders"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(data==="orders"){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await orders(admin,token,chatId,store,cb.from)». */ await orders(admin,token,chatId,store,cb.from)}else /* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id)». */ await ack(token,cb.id)}catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("callback",e)». */ console.error("callback",e);/* راهنما: این دستور متغیر/ثابت «m» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const m=String(e).includes("insufficient_stock")?"موجودی کافی نیست.":String(e).includes("cart_empty")?"سبد خرید خالی است.":"عملیات انجام نشد.";/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await ack(token,cb.id,m)». */ await ack(token,cb.id,m)}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")}
// راهنما: این دستور متغیر/ثابت «chatId، text، user» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const chatId=u.message?.chat?.id,text=u.message?.text?.trim(),user=u.message?.from;/* راهنما: این شرط بررسی می‌کند آیا «!chatId||!text» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!chatId||!text)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok");/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور متغیر/ثابت «start» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const start=text.match(/^\/start(?:\s+([A-Za-z0-9_-]{1,64}))?$/);/* راهنما: این شرط بررسی می‌کند آیا «start» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(start){/* راهنما: این دستور متغیر/ثابت «payload» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const payload=start[1]??"";/* راهنما: این شرط بررسی می‌کند آیا «store&&payload.startsWith("p_")&&payload.length>2» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(store&&payload.startsWith("p_")&&payload.length>2)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await product(admin,token,chatId,store,payload.slice(2))». */ await product(admin,token,chatId,store,payload.slice(2));else /* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await sendMenu(token,chatId,bot.welcomeMessage||"سلام! از منوی زیر انتخاب کنید.",roots)». */ await sendMenu(token,chatId,bot.welcomeMessage||"سلام! از منوی زیر انتخاب کنید.",roots)}else{/* راهنما: این دستور متغیر/ثابت «x» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const x=roots.find(v=>v.title===text);/* راهنما: این شرط بررسی می‌کند آیا «!x» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!x)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await sendMenu(token,chatId,"یکی از گزینه‌های منو را انتخاب کنید.",roots)». */ await sendMenu(token,chatId,"یکی از گزینه‌های منو را انتخاب کنید.",roots);else /* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await runMenuAction(admin,token,chatId,store,user,x,buttons)». */ await runMenuAction(admin,token,chatId,store,user,x,buttons)}}catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("message",e)». */ console.error("message",e);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await sendMenu(token,chatId,"در پردازش درخواست مشکلی پیش آمد.",roots)». */ await sendMenu(token,chatId,"در پردازش درخواست مشکلی پیش آمد.",roots)}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok")» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok")})};