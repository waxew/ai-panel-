/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";

// راهنما: این دستور متغیر/ثابت «corsHeaders» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"GET, POST, OPTIONS"};
// راهنما: این دستور متغیر/ثابت «json» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const json=(data:unknown,status=200)=>Response.json(data,{status,headers:corsHeaders});
// راهنما: این تابع «firstWorkspace» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function firstWorkspace(admin:any,userId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data,error}=await admin.from("WorkspaceMember").select("workspaceId").eq("userId",userId).limit(1);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data?.[0]?.workspaceId as string|undefined» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data?.[0]?.workspaceId as string|undefined;}
// راهنما: این تابع «ownsStaff» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function ownsStaff(admin:any,workspaceId:string,staffId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data,error}=await admin.from("BookingStaff").select("id").eq("id",staffId).eq("workspaceId",workspaceId).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «!!data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return!!data;}
// راهنما: این تابع «ownsService» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function ownsService(admin:any,workspaceId:string,serviceId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data,error}=await admin.from("BookingService").select("id").eq("id",serviceId).eq("workspaceId",workspaceId).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «!!data» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return!!data;}
// راهنما: این تابع «readDashboard» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function readDashboard(admin:any,workspaceId:string){/* راهنما: این دستور متغیر/ثابت «[services,staff,customers,appointments,setti…» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const[services,staff,customers,appointments,settings,staffServices,workingHours,timeOff]=await Promise.all([admin.from("BookingService").select("*").eq("workspaceId",workspaceId).order("createdAt",{ascending:true}),admin.from("BookingStaff").select("*").eq("workspaceId",workspaceId).order("createdAt",{ascending:true}),admin.from("BookingCustomer").select("*").eq("workspaceId",workspaceId).order("createdAt",{ascending:false}).limit(300),admin.from("BookingAppointment").select("id,workspaceId,customerId,serviceId,staffId,startsAt,endsAt,status,amount,depositAmount,paidAmount,currency,source,note,createdAt,BookingCustomer(fullName,phone,email),BookingService(title,durationMinutes,color),BookingStaff(name,roleTitle)").eq("workspaceId",workspaceId).order("startsAt",{ascending:true}).limit(600),admin.from("BookingSettings").select("*").eq("workspaceId",workspaceId).maybeSingle(),admin.from("BookingStaffService").select("staffId,serviceId,priceAmountOverride,durationMinutesOverride,BookingStaff!inner(workspaceId)").eq("BookingStaff.workspaceId",workspaceId),admin.from("BookingWorkingHour").select("id,staffId,weekday,startTime,endTime,isWorking,BookingStaff!inner(workspaceId)").eq("BookingStaff.workspaceId",workspaceId).order("weekday",{ascending:true}),admin.from("BookingTimeOff").select("*").eq("workspaceId",workspaceId).order("startsAt",{ascending:true}).limit(300)]);/* راهنما: این دستور متغیر/ثابت «error» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const error=[services.error,staff.error,customers.error,appointments.error,settings.error,staffServices.error,workingHours.error,timeOff.error].find(Boolean);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «{ok:true,services:services.data??[],staff:staff.data??[],customers:custome…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return{ok:true,services:services.data??[],staff:staff.data??[],customers:customers.data??[],appointments:appointments.data??[],settings:settings.data??null,staffServices:(staffServices.data??[]).map((x:any)=>({staffId:x.staffId,serviceId:x.serviceId,priceAmountOverride:x.priceAmountOverride,durationMinutesOverride:x.durationMinutesOverride})),workingHours:(workingHours.data??[]).map((x:any)=>({id:x.id,staffId:x.staffId,weekday:x.weekday,startTime:x.startTime,endTime:x.endTime,isWorking:x.isWorking})),timeOff:timeOff.data??[]};}

// راهنما: این دستور متغیر/ثابت «authenticated» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const authenticated=withSupabase({auth:"user"},async(request,ctx)=>{/* راهنما: این دستور متغیر/ثابت «userId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const userId=ctx.userClaims?.id;/* راهنما: این شرط بررسی می‌کند آیا «!userId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!userId)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ورود به حساب الزامی است."},401)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ورود به حساب الزامی است."},401);/* راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «workspaceId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const workspaceId=await firstWorkspace(admin,userId);/* راهنما: این شرط بررسی می‌کند آیا «!workspaceId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!workspaceId)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"فضای کاری پیدا نشد."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"فضای کاری پیدا نشد."},404);/* راهنما: این شرط بررسی می‌کند آیا «request.method==="GET"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(request.method==="GET"){/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}catch(e){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error(e)». */ console.error(e);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات نوبت‌دهی در دسترس نیست."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات نوبت‌دهی در دسترس نیست."},500)}}/* راهنما: این شرط بررسی می‌کند آیا «request.method!=="POST"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(request.method!=="POST")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Method not allowed"},405)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Method not allowed"},405);/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let body:any;/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «body=await request.json()». */ body=await request.json()}catch{/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"درخواست معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"درخواست معتبر نیست."},400)}/* راهنما: این دستور متغیر/ثابت «action» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const action=typeof body?.action==="string"?body.action:"";
// راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود.
try{
// راهنما: این شرط بررسی می‌کند آیا «action==="bootstrap"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="bootstrap"){/* راهنما: این دستور متغیر/ثابت «{data:existingService}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:existingService}=await admin.from("BookingService").select("id").eq("workspaceId",workspaceId).limit(1);/* راهنما: این شرط بررسی می‌کند آیا «!existingService?.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!existingService?.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingService").insert([{workspaceId,title:"مشاوره و ارزیابی",duration…». */ await admin.from("BookingService").insert([{workspaceId,title:"مشاوره و ارزیابی",durationMinutes:30,priceAmount:450000,color:"#6d5dfc"},{workspaceId,title:"سرویس استاندارد",durationMinutes:60,priceAmount:790000,color:"#11a779"},{workspaceId,title:"جلسه پیگیری",durationMinutes:45,priceAmount:590000,color:"#f59e0b"},{workspaceId,title:"سرویس VIP",durationMinutes:90,priceAmount:1290000,color:"#e34c8c"}]);/* راهنما: این دستور متغیر/ثابت «{data:existingStaff}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:existingStaff}=await admin.from("BookingStaff").select("id").eq("workspaceId",workspaceId).limit(1);/* راهنما: این شرط بررسی می‌کند آیا «!existingStaff?.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!existingStaff?.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingStaff").insert([{workspaceId,name:"سارا احمدی",roleTitle:"متخصص …». */ await admin.from("BookingStaff").insert([{workspaceId,name:"سارا احمدی",roleTitle:"متخصص ارشد",color:"#6659e5"},{workspaceId,name:"مینا رستگار",roleTitle:"کارشناس",color:"#11a779"},{workspaceId,name:"رویا زمانی",roleTitle:"متخصص",color:"#e34c8c"}]);/* راهنما: این دستور متغیر/ثابت «{data:allServices}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:allServices}=await admin.from("BookingService").select("id").eq("workspaceId",workspaceId).eq("isActive",true);/* راهنما: این دستور متغیر/ثابت «{data:allStaff}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:allStaff}=await admin.from("BookingStaff").select("id").eq("workspaceId",workspaceId).eq("isActive",true);/* راهنما: این شرط بررسی می‌کند آیا «allServices?.length&&allStaff?.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(allServices?.length&&allStaff?.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingStaffService").upsert(allStaff.flatMap((st:any)=>allServices.map…». */ await admin.from("BookingStaffService").upsert(allStaff.flatMap((st:any)=>allServices.map((sv:any)=>({staffId:st.id,serviceId:sv.id}))),{onConflict:"staffId,serviceId",ignoreDuplicates:true});/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(const st of allStaff??[])/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingWorkingHour").upsert([0,1,2,3,4,5,6].map(weekday=>({staffId:st.i…». */ await admin.from("BookingWorkingHour").upsert([0,1,2,3,4,5,6].map(weekday=>({staffId:st.id,weekday,isWorking:weekday!==5,startTime:"09:00",endTime:"18:00"})),{onConflict:"staffId,weekday",ignoreDuplicates:true});/* راهنما: این دستور متغیر/ثابت «{data:existingSettings}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:existingSettings}=await admin.from("BookingSettings").select("workspaceId").eq("workspaceId",workspaceId).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «!existingSettings» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!existingSettings)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingSettings").insert({workspaceId,publicSlug:`workspace-${workspace…». */ await admin.from("BookingSettings").insert({workspaceId,publicSlug:`workspace-${workspaceId.slice(0,8)}`});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId),201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId),201);}
// راهنما: این شرط بررسی می‌کند آیا «action==="create_service"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="create_service"){/* راهنما: این دستور متغیر/ثابت «title، durationMinutes، priceAmount» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const title=typeof body.title==="string"?body.title.trim().slice(0,120):"",durationMinutes=Number(body.durationMinutes),priceAmount=Number(body.priceAmount);/* راهنما: این شرط بررسی می‌کند آیا «!title||!Number.isInteger(durationMinutes)||durationMinutes<5||!Number.isSafeIn…» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!title||!Number.isInteger(durationMinutes)||durationMinutes<5||!Number.isSafeInteger(priceAmount)||priceAmount<0)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات خدمت معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات خدمت معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{data:created,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:created,error}=await admin.from("BookingService").insert({workspaceId,title,description:typeof body.description==="string"?body.description.trim().slice(0,2000)||null:null,durationMinutes,priceAmount,color:typeof body.color==="string"?body.color:"#6659e5"}).select("id").single();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این دستور متغیر/ثابت «{data:staff}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:staff}=await admin.from("BookingStaff").select("id").eq("workspaceId",workspaceId).eq("isActive",true);/* راهنما: این شرط بررسی می‌کند آیا «staff?.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(staff?.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingStaffService").insert(staff.map((x:any)=>({staffId:x.id,serviceI…». */ await admin.from("BookingStaffService").insert(staff.map((x:any)=>({staffId:x.id,serviceId:created.id})));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId),201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId),201);}
// راهنما: این شرط بررسی می‌کند آیا «action==="create_staff"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="create_staff"){/* راهنما: این دستور متغیر/ثابت «name» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const name=typeof body.name==="string"?body.name.trim().slice(0,120):"";/* راهنما: این شرط بررسی می‌کند آیا «!name» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!name)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"نام پرسنل الزامی است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"نام پرسنل الزامی است."},400);/* راهنما: این دستور متغیر/ثابت «{data:created,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:created,error}=await admin.from("BookingStaff").insert({workspaceId,name,roleTitle:typeof body.roleTitle==="string"?body.roleTitle.trim().slice(0,120)||null:null,phone:typeof body.phone==="string"?body.phone.trim().slice(0,40)||null:null,email:typeof body.email==="string"?body.email.trim().slice(0,160)||null:null}).select("id").single();/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingWorkingHour").insert([0,1,2,3,4,5,6].map(weekday=>({staffId:crea…». */ await admin.from("BookingWorkingHour").insert([0,1,2,3,4,5,6].map(weekday=>({staffId:created.id,weekday,isWorking:weekday!==5,startTime:"09:00",endTime:"18:00"})));/* راهنما: این دستور متغیر/ثابت «{data:services}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:services}=await admin.from("BookingService").select("id").eq("workspaceId",workspaceId).eq("isActive",true);/* راهنما: این شرط بررسی می‌کند آیا «services?.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(services?.length)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingStaffService").insert(services.map((s:any)=>({staffId:created.id…». */ await admin.from("BookingStaffService").insert(services.map((s:any)=>({staffId:created.id,serviceId:s.id})));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId),201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId),201);}
// راهنما: این شرط بررسی می‌کند آیا «action==="save_staff_services"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="save_staff_services"){/* راهنما: این دستور متغیر/ثابت «staffId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const staffId=typeof body.staffId==="string"?body.staffId:"";/* راهنما: این شرط بررسی می‌کند آیا «!staffId||!(await ownsStaff(admin,workspaceId,staffId))» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!staffId||!(await ownsStaff(admin,workspaceId,staffId)))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"پرسنل معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"پرسنل معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «rows، valid» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=Array.isArray(body.services)?body.services:[],valid=[] as any[];/* راهنما: این حلقه مجموعه‌ای از داده‌ها یا یک بازه را پیمایش می‌کند و منطق داخل بدنه را برای هر مرحله اجرا می‌کند. */ for(const row of rows){/* راهنما: این شرط بررسی می‌کند آیا «typeof row?.serviceId!=="string"||!(await ownsService(admin,workspaceId,row.ser…» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(typeof row?.serviceId!=="string"||!(await ownsService(admin,workspaceId,row.serviceId)))/* راهنما: این دستور ادامه دستورات مرحله فعلی حلقه را رد می‌کند و به تکرار بعدی می‌رود. */ continue;/* راهنما: این دستور متغیر/ثابت «price، duration» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const price=row.priceAmountOverride==null||row.priceAmountOverride===""?null:Number(row.priceAmountOverride),duration=row.durationMinutesOverride==null||row.durationMinutesOverride===""?null:Number(row.durationMinutesOverride);/* راهنما: این شرط بررسی می‌کند آیا «price!==null&&(!Number.isSafeInteger(price)||price<0)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(price!==null&&(!Number.isSafeInteger(price)||price<0))/* راهنما: این دستور ادامه دستورات مرحله فعلی حلقه را رد می‌کند و به تکرار بعدی می‌رود. */ continue;/* راهنما: این شرط بررسی می‌کند آیا «duration!==null&&(!Number.isInteger(duration)||duration<5||duration>1440)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(duration!==null&&(!Number.isInteger(duration)||duration<5||duration>1440))/* راهنما: این دستور ادامه دستورات مرحله فعلی حلقه را رد می‌کند و به تکرار بعدی می‌رود. */ continue;/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «valid.push({staffId,serviceId:row.serviceId,priceAmountOverride:price,durationMinutesOver…». */ valid.push({staffId,serviceId:row.serviceId,priceAmountOverride:price,durationMinutesOverride:duration});}/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await admin.from("BookingStaffService").delete().eq("staffId",staffId)». */ await admin.from("BookingStaffService").delete().eq("staffId",staffId);/* راهنما: این شرط بررسی می‌کند آیا «valid.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(valid.length){/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingStaffService").insert(valid);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}
// راهنما: این شرط بررسی می‌کند آیا «action==="save_working_hours"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="save_working_hours"){/* راهنما: این دستور متغیر/ثابت «staffId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const staffId=typeof body.staffId==="string"?body.staffId:"";/* راهنما: این شرط بررسی می‌کند آیا «!staffId||!(await ownsStaff(admin,workspaceId,staffId))» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!staffId||!(await ownsStaff(admin,workspaceId,staffId)))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"پرسنل معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"پرسنل معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «rows» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const rows=Array.isArray(body.hours)?body.hours:[];/* راهنما: این شرط بررسی می‌کند آیا «rows.length!==7» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(rows.length!==7)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"برنامه هفتگی کامل نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"برنامه هفتگی کامل نیست."},400);/* راهنما: این دستور متغیر/ثابت «normalized» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const normalized=rows.map((r:any)=>({staffId,weekday:Number(r.weekday),isWorking:r.isWorking===true,startTime:String(r.startTime||"09:00").slice(0,5),endTime:String(r.endTime||"18:00").slice(0,5)}));/* راهنما: این شرط بررسی می‌کند آیا «normalized.some((r:any)=>!Number.isInteger(r.weekday)||r.weekday<0||r.weekday>6…» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(normalized.some((r:any)=>!Number.isInteger(r.weekday)||r.weekday<0||r.weekday>6||!/^[0-2]\d:[0-5]\d$/.test(r.startTime)||!/^[0-2]\d:[0-5]\d$/.test(r.endTime)||r.endTime<=r.startTime))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ساعات کاری معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ساعات کاری معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingWorkingHour").upsert(normalized,{onConflict:"staffId,weekday"});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}
// راهنما: این شرط بررسی می‌کند آیا «action==="create_time_off"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="create_time_off"){/* راهنما: این دستور متغیر/ثابت «staffId، startsAt، endsAt» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const staffId=typeof body.staffId==="string"?body.staffId:"",startsAt=new Date(body.startsAt),endsAt=new Date(body.endsAt);/* راهنما: این شرط بررسی می‌کند آیا «!staffId||!(await ownsStaff(admin,workspaceId,staffId))||Number.isNaN(startsAt.…» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!staffId||!(await ownsStaff(admin,workspaceId,staffId))||Number.isNaN(startsAt.getTime())||Number.isNaN(endsAt.getTime())||endsAt<=startsAt)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"بازه مرخصی معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"بازه مرخصی معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingTimeOff").insert({workspaceId,staffId,startsAt:startsAt.toISOString(),endsAt:endsAt.toISOString(),type:["LEAVE","BLOCK","SICK","OTHER"].includes(body.type)?body.type:"LEAVE",note:typeof body.note==="string"?body.note.trim().slice(0,500)||null:null});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId),201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId),201);}
// راهنما: این شرط بررسی می‌کند آیا «action==="delete_time_off"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="delete_time_off"){/* راهنما: این دستور متغیر/ثابت «id» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const id=typeof body.id==="string"?body.id:"";/* راهنما: این شرط بررسی می‌کند آیا «!id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!id)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"شناسه معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"شناسه معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingTimeOff").delete().eq("id",id).eq("workspaceId",workspaceId);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}
// راهنما: این شرط بررسی می‌کند آیا «action==="update_customer"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="update_customer"){/* راهنما: این دستور متغیر/ثابت «id» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const id=typeof body.id==="string"?body.id:"";/* راهنما: این شرط بررسی می‌کند آیا «!id» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!id)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"مشتری معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"مشتری معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «tags» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const tags=Array.isArray(body.tags)?[...new Set(body.tags.map((x:any)=>String(x).trim().slice(0,40)).filter(Boolean))].slice(0,20):[];/* راهنما: این دستور متغیر/ثابت «patch» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const patch={fullName:typeof body.fullName==="string"?body.fullName.trim().slice(0,140):undefined,phone:typeof body.phone==="string"?body.phone.trim().slice(0,40):undefined,email:typeof body.email==="string"?body.email.trim().slice(0,160)||null:null,birthDate:typeof body.birthDate==="string"&&/^\d{4}-\d{2}-\d{2}$/.test(body.birthDate)?body.birthDate:null,notes:typeof body.notes==="string"?body.notes.trim().slice(0,3000)||null:null,tags,isVip:body.isVip===true,marketingOptIn:body.marketingOptIn!==false,updatedAt:new Date().toISOString()};/* راهنما: این شرط بررسی می‌کند آیا «!patch.fullName||!patch.phone» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!patch.fullName||!patch.phone)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"نام و شماره تماس الزامی است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"نام و شماره تماس الزامی است."},400);/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingCustomer").update(patch).eq("id",id).eq("workspaceId",workspaceId);/* راهنما: این شرط بررسی می‌کند آیا «error?.code==="23505"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error?.code==="23505")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این شماره تماس قبلاً برای مشتری دیگری ثبت شده است.…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این شماره تماس قبلاً برای مشتری دیگری ثبت شده است."},409);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}
// راهنما: این شرط بررسی می‌کند آیا «action==="create_appointment"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="create_appointment"){/* راهنما: این دستور متغیر/ثابت «customerName، phone، serviceId، staffId، startsAt» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const customerName=typeof body.customerName==="string"?body.customerName.trim().slice(0,140):"",phone=typeof body.phone==="string"?body.phone.trim().slice(0,40):"",serviceId=typeof body.serviceId==="string"?body.serviceId:"",staffId=typeof body.staffId==="string"?body.staffId:"",startsAt=new Date(body.startsAt);/* راهنما: این شرط بررسی می‌کند آیا «!customerName||!phone||!serviceId||!staffId||Number.isNaN(startsAt.getTime())» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!customerName||!phone||!serviceId||!staffId||Number.isNaN(startsAt.getTime()))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"اطلاعات نوبت ناقص است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"اطلاعات نوبت ناقص است."},400);/* راهنما: این دستور متغیر/ثابت «[{data:service},{data:staff},{data:assignmen…» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const[{data:service},{data:staff},{data:assignment}]=await Promise.all([admin.from("BookingService").select("id,durationMinutes,priceAmount,currency,isActive").eq("id",serviceId).eq("workspaceId",workspaceId).maybeSingle(),admin.from("BookingStaff").select("id,isActive").eq("id",staffId).eq("workspaceId",workspaceId).maybeSingle(),admin.from("BookingStaffService").select("priceAmountOverride,durationMinutesOverride").eq("staffId",staffId).eq("serviceId",serviceId).maybeSingle()]);/* راهنما: این شرط بررسی می‌کند آیا «!service?.isActive||!staff?.isActive||!assignment» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!service?.isActive||!staff?.isActive||!assignment)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این خدمت برای این پرسنل فعال نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این خدمت برای این پرسنل فعال نیست."},400);/* راهنما: این دستور متغیر/ثابت «duration، amount، endsAt» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const duration=Number(assignment.durationMinutesOverride??service.durationMinutes),amount=Number(assignment.priceAmountOverride??service.priceAmount),endsAt=new Date(startsAt.getTime()+duration*60000);/* راهنما: این دستور متغیر/ثابت «[{data:overlap,error:overlapError},{data:blo…» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const[{data:overlap,error:overlapError},{data:blocked,error:blockedError}]=await Promise.all([admin.from("BookingAppointment").select("id").eq("workspaceId",workspaceId).eq("staffId",staffId).in("status",["PENDING","CONFIRMED"]).lt("startsAt",endsAt.toISOString()).gt("endsAt",startsAt.toISOString()).limit(1),admin.from("BookingTimeOff").select("id").eq("workspaceId",workspaceId).eq("staffId",staffId).lt("startsAt",endsAt.toISOString()).gt("endsAt",startsAt.toISOString()).limit(1)]);/* راهنما: این شرط بررسی می‌کند آیا «overlapError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(overlapError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw overlapError;/* راهنما: این شرط بررسی می‌کند آیا «blockedError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(blockedError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw blockedError;/* راهنما: این شرط بررسی می‌کند آیا «overlap?.length||blocked?.length» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(overlap?.length||blocked?.length)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این بازه زمانی برای این پرسنل در دسترس نیست."},409)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این بازه زمانی برای این پرسنل در دسترس نیست."},409);/* راهنما: این دستور متغیر/ثابت «{data:customer,error:customerError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{data:customer,error:customerError}=await admin.from("BookingCustomer").upsert({workspaceId,fullName:customerName,phone,email:typeof body.email==="string"?body.email.trim().slice(0,160)||null:null},{onConflict:"workspaceId,phone"}).select("id").single();/* راهنما: این شرط بررسی می‌کند آیا «customerError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(customerError)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw customerError;/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingAppointment").insert({workspaceId,customerId:customer.id,serviceId,staffId,startsAt:startsAt.toISOString(),endsAt:endsAt.toISOString(),amount,currency:service.currency??"IRR",status:"CONFIRMED",source:"PANEL",note:typeof body.note==="string"?body.note.trim().slice(0,2000)||null:null,createdByUserId:userId});/* راهنما: این شرط بررسی می‌کند آیا «error?.code==="23P01"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error?.code==="23P01")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"این زمان همین الان رزرو شد."},409)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"این زمان همین الان رزرو شد."},409);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId),201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId),201);}
// راهنما: این شرط بررسی می‌کند آیا «action==="update_appointment_status"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="update_appointment_status"){/* راهنما: این دستور متغیر/ثابت «id، status» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const id=typeof body.id==="string"?body.id:"",status=["PENDING","CONFIRMED","DONE","CANCELLED","NO_SHOW"].includes(body.status)?body.status:"";/* راهنما: این شرط بررسی می‌کند آیا «!id||!status» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!id||!status)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"وضعیت معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"وضعیت معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «patch» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const patch:Record<string,unknown>={status,updatedAt:new Date().toISOString()};/* راهنما: این شرط بررسی می‌کند آیا «status==="DONE"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(status==="DONE")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «patch.completedAt=new Date().toISOString()». */ patch.completedAt=new Date().toISOString();/* راهنما: این شرط بررسی می‌کند آیا «status==="CANCELLED"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(status==="CANCELLED")/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «patch.cancelledAt=new Date().toISOString()». */ patch.cancelledAt=new Date().toISOString();/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingAppointment").update(patch).eq("id",id).eq("workspaceId",workspaceId);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}
// راهنما: این شرط بررسی می‌کند آیا «action==="save_settings"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
if(action==="save_settings"){/* راهنما: این دستور متغیر/ثابت «row» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const row={workspaceId,slotIntervalMinutes:Number(body.slotIntervalMinutes??15),minBookingNoticeMinutes:Number(body.minBookingNoticeMinutes??120),cancellationNoticeMinutes:Number(body.cancellationNoticeMinutes??360),allowCustomerCancellation:body.allowCustomerCancellation!==false,requireDeposit:body.requireDeposit===true,defaultDepositPercent:Number(body.defaultDepositPercent??30),publicBookingEnabled:body.publicBookingEnabled!==false,reminders:typeof body.reminders==="object"&&body.reminders?body.reminders:{confirmation:true,before24h:true,whatsapp:false},updatedAt:new Date().toISOString()};/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const{error}=await admin.from("BookingSettings").upsert(row,{onConflict:"workspaceId"});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw error;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json(await readDashboard(admin,workspaceId))» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json(await readDashboard(admin,workspaceId));}
// راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"عملیات شناخته‌شده نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
return json({ok:false,message:"عملیات شناخته‌شده نیست."},400);}catch(error){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error("booking-manage",error)». */ console.error("booking-manage",error);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"عملیات نوبت‌دهی انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"عملیات نوبت‌دهی انجام نشد."},500)}});
// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{async fetch(request:Request){/* راهنما: این شرط بررسی می‌کند آیا «request.method==="OPTIONS"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(request.method==="OPTIONS")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok",{headers:corsHeaders})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok",{headers:corsHeaders});/* راهنما: این دستور متغیر/ثابت «response» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const response=await authenticated(request);/* راهنما: این دستور متغیر/ثابت «headers» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const headers=new Headers(response.headers);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «Object.entries(corsHeaders).forEach(([k,v])=>headers.set(k,v))». */ Object.entries(corsHeaders).forEach(([k,v])=>headers.set(k,v));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response(response.body,{status:response.status,headers})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response(response.body,{status:response.status,headers});}};
