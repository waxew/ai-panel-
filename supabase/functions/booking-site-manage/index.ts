/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";
// راهنما: این دستور متغیر/ثابت «cors» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, apikey, content-type","Access-Control-Allow-Methods":"GET,POST,OPTIONS"};
// راهنما: این دستور متغیر/ثابت «json» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const json=(d:unknown,s=200)=>Response.json(d,{status:s,headers:cors});
// راهنما: این تابع «workspace» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function workspace(admin:any,userId:string){/* راهنما: این دستور متغیر/ثابت «{data,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data,error}=await admin.from("WorkspaceMember").select("workspaceId,role").eq("userId",userId).limit(1);/* راهنما: این شرط بررسی می‌کند آیا «error||!data?.[0]» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error||!data?.[0])/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «null» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return null;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data[0] as {workspaceId:string;role:string}» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data[0] as {workspaceId:string;role:string};}
// راهنما: این دستور متغیر/ثابت «handler» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const handler=withSupabase({auth:"user"},async(req,ctx)=>{/* راهنما: این دستور متغیر/ثابت «userId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const userId=ctx.userClaims?.id;/* راهنما: این شرط بررسی می‌کند آیا «!userId» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!userId)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ورود الزامی است."},401)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ورود الزامی است."},401);/* راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «member» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const member=await workspace(admin,userId);/* راهنما: این شرط بررسی می‌کند آیا «!member» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!member)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Workspace پیدا نشد."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Workspace پیدا نشد."},404);/* راهنما: این دستور متغیر/ثابت «wid» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const wid=member.workspaceId;
 // راهنما: این شرط بررسی می‌کند آیا «req.method==="GET"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(req.method==="GET"){/* راهنما: این دستور متغیر/ثابت «[siteR,msgR,svcR,staffR]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [siteR,msgR,svcR,staffR]=await Promise.all([admin.from("BookingBusinessSite").select("*").eq("workspaceId",wid).maybeSingle(),admin.from("BookingInboxMessage").select("*").eq("workspaceId",wid).order("createdAt",{ascending:false}).limit(200),admin.from("BookingService").select("id,title,description,durationMinutes,priceAmount,currency,color,isActive").eq("workspaceId",wid).eq("isActive",true),admin.from("BookingStaff").select("id,name,roleTitle,color,isActive").eq("workspaceId",wid).eq("isActive",true)]);/* راهنما: این دستور متغیر/ثابت «err» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const err=[siteR,msgR,svcR,staffR].map((x:any)=>x.error).find(Boolean);/* راهنما: این شرط بررسی می‌کند آیا «err» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(err)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"دریافت اطلاعات سایت انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"دریافت اطلاعات سایت انجام نشد."},500);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true,site:siteR.data,messages:msgR.data||[],services:svcR.data||[…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true,site:siteR.data,messages:msgR.data||[],services:svcR.data||[],staff:staffR.data||[]});}
 // راهنما: این شرط بررسی می‌کند آیا «req.method!=="POST"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(req.method!=="POST")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Method not allowed"},405)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Method not allowed"},405);/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const body=await req.json().catch(()=>({}));/* راهنما: این دستور متغیر/ثابت «action» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const action=String(body.action||"");
 // راهنما: این شرط بررسی می‌کند آیا «action==="save_site"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(action==="save_site"){/* راهنما: این شرط بررسی می‌کند آیا «!["ADMIN","SUPER_ADMIN"].includes(member.role)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!["ADMIN","SUPER_ADMIN"].includes(member.role))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"دسترسی مدیر لازم است."},403)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"دسترسی مدیر لازم است."},403);/* راهنما: این دستور متغیر/ثابت «current» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const current=(await admin.from("BookingBusinessSite").select("slug").eq("workspaceId",wid).maybeSingle()).data;/* راهنما: این دستور متغیر/ثابت «slug» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const slug=String(body.slug||current?.slug||"").trim().toLowerCase();/* راهنما: این شرط بررسی می‌کند آیا «!/^[a-z0-9][a-z0-9-]{2,63}$/.test(slug)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!/^[a-z0-9][a-z0-9-]{2,63}$/.test(slug))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Slug معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Slug معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «payload» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const payload={workspaceId:wid,slug,enabled:body.enabled!==false,brandName:String(body.brandName||"کسب‌وکار من").slice(0,120),tagline:String(body.tagline||"").slice(0,220)||null,aboutText:String(body.aboutText||"").slice(0,3000)||null,phone:String(body.phone||"").slice(0,40)||null,email:String(body.email||"").slice(0,160)||null,address:String(body.address||"").slice(0,500)||null,instagramUrl:String(body.instagramUrl||"").slice(0,500)||null,whatsappUrl:String(body.whatsappUrl||"").slice(0,500)||null,websiteUrl:String(body.websiteUrl||"").slice(0,500)||null,logoUrl:String(body.logoUrl||"").slice(0,1000)||null,coverUrl:String(body.coverUrl||"").slice(0,1000)||null,accentColor:/^#[0-9a-fA-F]{6}$/.test(String(body.accentColor||""))?String(body.accentColor):"#6659e5",theme:body.theme==="DARK"?"DARK":"LIGHT",showServices:body.showServices!==false,showStaff:body.showStaff!==false,showBooking:body.showBooking!==false,showContact:body.showContact!==false,showLoyalty:body.showLoyalty!==false,metaTitle:String(body.metaTitle||"").slice(0,160)||null,metaDescription:String(body.metaDescription||"").slice(0,320)||null,updatedAt:new Date().toISOString()};/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {error}=await admin.from("BookingBusinessSite").upsert(payload,{onConflict:"workspaceId"});/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «console.error(error)». */ console.error(error);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:error.code==="23505"?"این آدرس سایت قبلاً استفاده ش…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:error.code==="23505"?"این آدرس سایت قبلاً استفاده شده است.":"ذخیره سایت انجام نشد."},409)}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true});}
 // راهنما: این شرط بررسی می‌کند آیا «action==="update_message"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(action==="update_message"){/* راهنما: این دستور متغیر/ثابت «id» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const id=String(body.id||"");/* راهنما: این دستور متغیر/ثابت «status» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const status=String(body.status||"");/* راهنما: این شرط بررسی می‌کند آیا «!["NEW","OPEN","DONE","ARCHIVED","SPAM"].includes(status)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!["NEW","OPEN","DONE","ARCHIVED","SPAM"].includes(status))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"وضعیت نامعتبر است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"وضعیت نامعتبر است."},400);/* راهنما: این دستور متغیر/ثابت «patch» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const patch:any={status,updatedAt:new Date().toISOString()};/* راهنما: این شرط بررسی می‌کند آیا «status==="DONE"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(status==="DONE"){/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «patch.handledByUserId=userId». */ patch.handledByUserId=userId;/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «patch.handledAt=new Date().toISOString()». */ patch.handledAt=new Date().toISOString();}/* راهنما: این دستور متغیر/ثابت «{error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {error}=await admin.from("BookingInboxMessage").update(patch).eq("id",id).eq("workspaceId",wid);/* راهنما: این شرط بررسی می‌کند آیا «error» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"به‌روزرسانی پیام انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"به‌روزرسانی پیام انجام نشد."},500);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true});}
 // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Action not found"},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
 return json({ok:false,message:"Action not found"},404);
});
// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{async fetch(req:Request){/* راهنما: این شرط بررسی می‌کند آیا «req.method==="OPTIONS"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(req.method==="OPTIONS")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok",{headers:cors})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok",{headers:cors});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «handler(req)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return handler(req);}};
