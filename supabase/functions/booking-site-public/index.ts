/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور فایل/ماژول را از ماژول «jsr:@supabase/functions-js/edge-runtime.d.ts» وارد می‌کند تا در این فایل قابل استفاده باشد.
import "jsr:@supabase/functions-js/edge-runtime.d.ts";
// راهنما: این دستور { withSupabase } را از ماژول «npm:@supabase/server@1.4.1» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { withSupabase } from "npm:@supabase/server@1.4.1";
// راهنما: این دستور متغیر/ثابت «cors» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, apikey, content-type","Access-Control-Allow-Methods":"GET,POST,OPTIONS"};/* راهنما: این دستور متغیر/ثابت «json» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const json=(d:unknown,s=200)=>Response.json(d,{status:s,headers:{...cors,"Cache-Control":"no-store"}});
// راهنما: این دستور متغیر/ثابت «handler» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const handler=withSupabase({auth:"none"},async(req,ctx)=>{/* راهنما: این دستور متغیر/ثابت «admin» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const admin=ctx.supabaseAdmin;/* راهنما: این دستور متغیر/ثابت «url» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const url=new URL(req.url);/* راهنما: این دستور متغیر/ثابت «slug» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const slug=(url.searchParams.get("slug")||"").trim().toLowerCase();/* راهنما: این شرط بررسی می‌کند آیا «!/^[a-z0-9][a-z0-9-]{2,63}$/.test(slug)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!/^[a-z0-9][a-z0-9-]{2,63}$/.test(slug))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"آدرس سایت معتبر نیست."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"آدرس سایت معتبر نیست."},400);/* راهنما: این دستور متغیر/ثابت «{data:site,error}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {data:site,error}=await admin.from("BookingBusinessSite").select("*").eq("slug",slug).eq("enabled",true).maybeSingle();/* راهنما: این شرط بررسی می‌کند آیا «error||!site» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(error||!site)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"سایت پیدا نشد."},404)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"سایت پیدا نشد."},404);/* راهنما: این دستور متغیر/ثابت «wid» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const wid=site.workspaceId;
 // راهنما: این شرط بررسی می‌کند آیا «req.method==="GET"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(req.method==="GET"){/* راهنما: این دستور متغیر/ثابت «[svcR,staffR,setR,loyaltyR]» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const [svcR,staffR,setR,loyaltyR]=await Promise.all([site.showServices?admin.from("BookingService").select("id,title,description,durationMinutes,priceAmount,currency,color").eq("workspaceId",wid).eq("isActive",true).order("createdAt",{ascending:true}):Promise.resolve({data:[],error:null}),site.showStaff?admin.from("BookingStaff").select("id,name,roleTitle,color").eq("workspaceId",wid).eq("isActive",true).order("createdAt",{ascending:true}):Promise.resolve({data:[],error:null}),admin.from("BookingSettings").select("publicBookingEnabled,publicSlug,timezone").eq("workspaceId",wid).maybeSingle(),site.showLoyalty?admin.from("BookingLoyaltySettings").select("enabled,silverThreshold,goldThreshold,vipThreshold").eq("workspaceId",wid).maybeSingle():Promise.resolve({data:null,error:null})]);/* راهنما: این دستور متغیر/ثابت «err» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const err=[svcR,staffR,setR,loyaltyR].map((x:any)=>x.error).find(Boolean);/* راهنما: این شرط بررسی می‌کند آیا «err» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(err)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"دریافت سایت انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"دریافت سایت انجام نشد."},500);/* راهنما: این دستور متغیر/ثابت «safeSite» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const safeSite={slug:site.slug,brandName:site.brandName,tagline:site.tagline,aboutText:site.aboutText,phone:site.phone,email:site.email,address:site.address,instagramUrl:site.instagramUrl,whatsappUrl:site.whatsappUrl,websiteUrl:site.websiteUrl,logoUrl:site.logoUrl,coverUrl:site.coverUrl,accentColor:site.accentColor,theme:site.theme,showServices:site.showServices,showStaff:site.showStaff,showBooking:site.showBooking,showContact:site.showContact,showLoyalty:site.showLoyalty,metaTitle:site.metaTitle,metaDescription:site.metaDescription};/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true,site:safeSite,services:svcR.data||[],staff:staffR.data||[],b…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true,site:safeSite,services:svcR.data||[],staff:staffR.data||[],booking:setR.data||null,loyalty:loyaltyR.data||null});}
 // راهنما: این شرط بررسی می‌کند آیا «req.method==="POST"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(req.method==="POST"){/* راهنما: این دستور متغیر/ثابت «body» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const body=await req.json().catch(()=>({}));/* راهنما: این دستور متغیر/ثابت «name، phone، email، subject، message، honeypot» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const name=String(body.name||"").trim().slice(0,120),phone=String(body.phone||"").trim().slice(0,40),email=String(body.email||"").trim().slice(0,160),subject=String(body.subject||"").trim().slice(0,160),message=String(body.message||"").trim().slice(0,3000),honeypot=String(body.company||"").trim();/* راهنما: این شرط بررسی می‌کند آیا «honeypot» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(honeypot)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true});/* راهنما: این شرط بررسی می‌کند آیا «!message||(!phone&&!email)» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!message||(!phone&&!email))/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"متن پیام و حداقل یک راه تماس لازم است."},400)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"متن پیام و حداقل یک راه تماس لازم است."},400);/* راهنما: این دستور متغیر/ثابت «customerId» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ let customerId:null|string=null;/* راهنما: این شرط بررسی می‌کند آیا «phone» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(phone){/* راهنما: این دستور متغیر/ثابت «existing» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const existing=await admin.from("BookingCustomer").select("id").eq("workspaceId",wid).eq("phone",phone).maybeSingle();/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «customerId=existing.data?.id||null». */ customerId=existing.data?.id||null;}/* راهنما: این دستور متغیر/ثابت «{error:insertError}» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const {error:insertError}=await admin.from("BookingInboxMessage").insert({workspaceId:wid,source:"WEBSITE",customerId,name:name||null,phone:phone||null,email:email||null,subject:subject||null,body:message,status:"NEW",priority:"NORMAL"});/* راهنما: این شرط بررسی می‌کند آیا «insertError» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(insertError)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"ارسال پیام انجام نشد."},500)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"ارسال پیام انجام نشد."},500);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:true,message:"پیام شما ثبت شد."},201)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:true,message:"پیام شما ثبت شد."},201);}/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «json({ok:false,message:"Method not allowed"},405)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return json({ok:false,message:"Method not allowed"},405);});
// راهنما: این دستور از نوع ExportAssignment بخشی از کنترل جریان یا تعریف منطق این فایل است.
export default{async fetch(req:Request){/* راهنما: این شرط بررسی می‌کند آیا «req.method==="OPTIONS"» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(req.method==="OPTIONS")/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «new Response("ok",{headers:cors})» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return new Response("ok",{headers:cors});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «handler(req)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return handler(req);}};
