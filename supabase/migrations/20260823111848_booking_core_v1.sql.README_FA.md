# راهنمای خط‌به‌خط `20260823111848_booking_core_v1.sql`

> SQL می‌تواند شامل Function body و رشته‌های چندخطی باشد؛ تزریق کامنت خودکار بین همه خطوط ممکن است معنی Migration را عوض کند. برای حفظ دیتابیس، توضیح خط‌به‌خط در این فایل کنار Migration ذخیره می‌شود.

- خط 1: `create table if not exists public."BookingSettings" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 2: `"workspaceId" text primary key references public."Workspace"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 3: `"timezone" text not null default 'Asia/Tehran',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 4: `"slotIntervalMinutes" integer not null default 15 check ("slotIntervalMinutes" between 5 and 180),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 5: `"minBookingNoticeMinutes" integer not null default 120 check ("minBookingNoticeMinutes" between 0 and 10080),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 6: `"cancellationNoticeMinutes" integer not null default 360 check ("cancellationNoticeMinutes" between 0 and 43200),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 7: `"allowCustomerCancellation" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 8: `"requireDeposit" boolean not null default false,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 9: `"defaultDepositPercent" integer not null default 30 check ("defaultDepositPercent" between 0 and 100),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 10: `"publicBookingEnabled" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 11: `"publicSlug" text unique,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 12: `"reminders" jsonb not null default '{"confirmation":true,"before24h":true,"whatsapp":false}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 13: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 14: `"updatedAt" timestamptz not null default now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 15: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 17: `create table if not exists public."BookingService" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 18: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 19: `"workspaceId" text not null references public."Workspace"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 20: `title text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 21: `description text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 22: `"durationMinutes" integer not null check ("durationMinutes" between 5 and 1440),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 23: `"bufferMinutes" integer not null default 0 check ("bufferMinutes" between 0 and 240),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 24: `"priceAmount" bigint not null default 0 check ("priceAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 25: `currency text not null default 'IRR',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 26: `"depositPercent" integer check ("depositPercent" is null or "depositPercent" between 0 and 100),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 27: `color text not null default '#6659e5',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 28: `"isActive" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 29: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 30: `"updatedAt" timestamptz not null default now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 31: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 33: `create table if not exists public."BookingStaff" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 34: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 35: `"workspaceId" text not null references public."Workspace"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 36: `name text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 37: `"roleTitle" text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 38: `phone text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 39: `email text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 40: `color text not null default '#11a779',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 41: `"isActive" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 42: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 43: `"updatedAt" timestamptz not null default now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 44: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 46: `create table if not exists public."BookingStaffService" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 47: `"staffId" text not null references public."BookingStaff"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 48: `"serviceId" text not null references public."BookingService"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 49: `"priceAmountOverride" bigint check ("priceAmountOverride" is null or "priceAmountOverride" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 50: `"durationMinutesOverride" integer check ("durationMinutesOverride" is null or "durationMinutesOverride" between 5 and 1440),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 51: `primary key ("staffId", "serviceId")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 52: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 54: `create table if not exists public."BookingWorkingHour" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 55: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 56: `"staffId" text not null references public."BookingStaff"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 57: `weekday smallint not null check (weekday between 0 and 6),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 58: `"startTime" time not null default '09:00',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 59: `"endTime" time not null default '18:00',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 60: `"isWorking" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 61: `unique ("staffId", weekday),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 62: `check ("endTime" > "startTime")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 63: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 65: `create table if not exists public."BookingCustomer" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 66: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 67: `"workspaceId" text not null references public."Workspace"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 68: `"fullName" text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 69: `phone text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 70: `email text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 71: `notes text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 72: `metadata jsonb not null default '{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 73: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 74: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 75: `unique ("workspaceId", phone)` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 76: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 78: `create table if not exists public."BookingAppointment" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 79: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 80: `"workspaceId" text not null references public."Workspace"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 81: `"customerId" text not null references public."BookingCustomer"(id) on delete restrict,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 82: `"serviceId" text not null references public."BookingService"(id) on delete restrict,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 83: `"staffId" text not null references public."BookingStaff"(id) on delete restrict,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 84: `"startsAt" timestamptz not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 85: `"endsAt" timestamptz not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 86: `status text not null default 'CONFIRMED' check (status in ('PENDING','CONFIRMED','DONE','CANCELLED','NO_SHOW')),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 87: `"amount" bigint not null default 0 check ("amount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 88: `"depositAmount" bigint not null default 0 check ("depositAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 89: `"paidAmount" bigint not null default 0 check ("paidAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 90: `currency text not null default 'IRR',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 91: `source text not null default 'PANEL' check (source in ('PANEL','PUBLIC_BOOKING','TELEGRAM','INSTAGRAM','WHATSAPP','IMPORT')),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 92: `note text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 93: `"createdByUserId" text references public."User"(id) on delete set null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 94: `"completedAt" timestamptz,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 95: `"cancelledAt" timestamptz,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 96: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 97: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 98: `check ("endsAt" > "startsAt")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 99: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 101: `create index if not exists "BookingService_workspaceId_idx" on public."BookingService"("workspaceId");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 102: `create index if not exists "BookingStaff_workspaceId_idx" on public."BookingStaff"("workspaceId");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 103: `create index if not exists "BookingCustomer_workspaceId_idx" on public."BookingCustomer"("workspaceId");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 104: `create index if not exists "BookingAppointment_workspace_starts_idx" on public."BookingAppointment"("workspaceId", "startsAt");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 105: `create index if not exists "BookingAppointment_staff_starts_idx" on public."BookingAppointment"("staffId", "startsAt");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 106: `create index if not exists "BookingAppointment_customer_starts_idx" on public."BookingAppointment"("customerId", "startsAt");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 107: `create index if not exists "BookingAppointment_status_starts_idx" on public."BookingAppointment"(status, "startsAt");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 109: `alter table public."BookingSettings" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 110: `alter table public."BookingService" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 111: `alter table public."BookingStaff" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 112: `alter table public."BookingStaffService" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 113: `alter table public."BookingWorkingHour" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 114: `alter table public."BookingCustomer" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 115: `alter table public."BookingAppointment" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 117: `revoke all on public."BookingSettings" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 118: `revoke all on public."BookingService" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 119: `revoke all on public."BookingStaff" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 120: `revoke all on public."BookingStaffService" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 121: `revoke all on public."BookingWorkingHour" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 122: `revoke all on public."BookingCustomer" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 123: `revoke all on public."BookingAppointment" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.