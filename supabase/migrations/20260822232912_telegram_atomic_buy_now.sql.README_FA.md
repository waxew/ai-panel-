# راهنمای خط‌به‌خط `20260822232912_telegram_atomic_buy_now.sql`

> SQL می‌تواند شامل Function body و رشته‌های چندخطی باشد؛ تزریق کامنت خودکار بین همه خطوط ممکن است معنی Migration را عوض کند. برای حفظ دیتابیس، توضیح خط‌به‌خط در این فایل کنار Migration ذخیره می‌شود.

- خط 1: `create or replace function public.telegram_create_store_order(` — این دستور یک Function سمت PostgreSQL تعریف یا جایگزین می‌کند.
- خط 2: `p_store_id text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 3: `p_item_id text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 4: `p_external_user_id text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 5: `p_username text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 6: `p_display_name text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 7: `p_external_conversation_id text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 8: `p_idempotency_key text` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 9: `)` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 10: `returns jsonb` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 11: `language plpgsql` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 12: `security definer` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 13: `set search_path = ''` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 14: `as $$` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 15: `declare` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 16: `v_store public."Store"%rowtype;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 17: `v_item public."StoreItem"%rowtype;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 18: `v_customer_id text;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 19: `v_order_id text;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 20: `v_existing public."StoreOrder"%rowtype;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 21: `begin` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 22: `select * into v_store` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 23: `from public."Store"` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 24: `where id = p_store_id and status = 'ACTIVE'::public."StoreStatus";` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 26: `if not found then` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 27: `raise exception 'store_unavailable';` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 28: `end if;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 30: `select * into v_item` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 31: `from public."StoreItem"` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 32: `where id = p_item_id` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 33: `and "storeId" = p_store_id` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 34: `and "isActive" = true` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 35: `for update;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 37: `if not found then` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 38: `raise exception 'item_unavailable';` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 39: `end if;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 41: `if v_item."inventoryCount" is not null and v_item."inventoryCount" <= 0 then` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 42: `raise exception 'out_of_stock';` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 43: `end if;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 45: `insert into public."StoreCustomer" (` — این دستور داده جدید در جدول درج می‌کند.
- خط 46: `id, "storeId", platform, "externalUserId", username, "displayName", metadata,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 47: `"lastSeenAt", "createdAt", "updatedAt"` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 48: `) values (` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 49: `gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 50: `p_store_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 51: `'telegram',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 52: `p_external_user_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 53: `nullif(p_username, ''),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 54: `nullif(p_display_name, ''),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 55: `'{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 56: `now(), now(), now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 57: `)` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 58: `on conflict ("storeId", platform, "externalUserId")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 59: `do update set` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 60: `username = excluded.username,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 61: `"displayName" = excluded."displayName",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 62: `"lastSeenAt" = now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 63: `"updatedAt" = now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 64: `returning id into v_customer_id;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 66: `if p_idempotency_key is not null then` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 67: `select * into v_existing` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 68: `from public."StoreOrder"` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 69: `where "storeId" = p_store_id` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 70: `and "idempotencyKey" = p_idempotency_key` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 71: `limit 1;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 73: `if found then` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 74: `return jsonb_build_object(` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 75: `'orderId', v_existing.id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 76: `'status', v_existing.status,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 77: `'totalAmount', v_existing."totalAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 78: `'currency', v_existing.currency,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 79: `'replayed', true` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 80: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 81: `end if;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 82: `end if;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 84: `v_order_id := gen_random_uuid()::text;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 86: `insert into public."StoreOrder" (` — این دستور داده جدید در جدول درج می‌کند.
- خط 87: `id, "storeId", "customerId", "sourcePlatform", "externalConversationId",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 88: `status, "subtotalAmount", "discountAmount", "totalAmount", currency,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 89: `note, "idempotencyKey", "createdAt", "updatedAt"` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 90: `) values (` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 91: `v_order_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 92: `p_store_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 93: `v_customer_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 94: `'telegram',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 95: `p_external_conversation_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 96: `'AWAITING_PAYMENT'::public."StoreOrderStatus",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 97: `v_item."priceAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 98: `0,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 99: `v_item."priceAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 100: `v_item.currency,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 101: `null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 102: `p_idempotency_key,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 103: `now(), now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 104: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 106: `insert into public."StoreOrderItem" (` — این دستور داده جدید در جدول درج می‌کند.
- خط 107: `id, "orderId", "itemId", "titleSnapshot", "skuSnapshot", "unitPriceAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 108: `quantity, "lineTotalAmount", metadata, "createdAt"` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 109: `) values (` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 110: `gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 111: `v_order_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 112: `v_item.id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 113: `v_item.title,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 114: `v_item.sku,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 115: `v_item."priceAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 116: `1,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 117: `v_item."priceAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 118: `'{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 119: `now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 120: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 122: `return jsonb_build_object(` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 123: `'orderId', v_order_id,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 124: `'status', 'AWAITING_PAYMENT',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 125: `'totalAmount', v_item."priceAmount",` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 126: `'currency', v_item.currency,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 127: `'replayed', false` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 128: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 129: `end;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 130: `$$;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 132: `revoke all on function public.telegram_create_store_order(text,text,text,text,text,text,text) from public;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 133: `revoke all on function public.telegram_create_store_order(text,text,text,text,text,text,text) from anon;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 134: `revoke all on function public.telegram_create_store_order(text,text,text,text,text,text,text) from authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 135: `grant execute on function public.telegram_create_store_order(text,text,text,text,text,text,text) to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.