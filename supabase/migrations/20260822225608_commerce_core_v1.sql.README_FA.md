# راهنمای خط‌به‌خط `20260822225608_commerce_core_v1.sql`

> SQL می‌تواند شامل Function body و رشته‌های چندخطی باشد؛ تزریق کامنت خودکار بین همه خطوط ممکن است معنی Migration را عوض کند. برای حفظ دیتابیس، توضیح خط‌به‌خط در این فایل کنار Migration ذخیره می‌شود.

- خط 1: `do $$ begin` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 2: `create type public."StoreStatus" as enum ('DRAFT','ACTIVE','PAUSED');` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 3: `exception when duplicate_object then null; end $$;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 5: `do $$ begin` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 6: `create type public."StoreItemType" as enum ('DIGITAL','PHYSICAL','SERVICE');` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 7: `exception when duplicate_object then null; end $$;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 9: `do $$ begin` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 10: `create type public."StoreCartStatus" as enum ('ACTIVE','CONVERTED','ABANDONED');` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 11: `exception when duplicate_object then null; end $$;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 13: `do $$ begin` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 14: `create type public."StoreOrderStatus" as enum ('NEW','AWAITING_PAYMENT','PAID','PROCESSING','COMPLETED','CANCELLED','REFUNDED');` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 15: `exception when duplicate_object then null; end $$;` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 17: `create table if not exists public."Store" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 18: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 19: `"workspaceId" text not null unique references public."Workspace"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 20: `name text not null default 'فروشگاه من',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 21: `currency text not null default 'IRR',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 22: `status public."StoreStatus" not null default 'ACTIVE',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 23: `settings jsonb not null default '{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 24: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 25: `"updatedAt" timestamptz not null default now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 26: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 28: `create table if not exists public."StoreCategory" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 29: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 30: `"storeId" text not null references public."Store"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 31: `title text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 32: `slug text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 33: `"sortOrder" integer not null default 0,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 34: `"isActive" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 35: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 36: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 37: `unique ("storeId", slug)` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 38: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 40: `create table if not exists public."StoreItem" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 41: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 42: `"storeId" text not null references public."Store"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 43: `"categoryId" text references public."StoreCategory"(id) on delete set null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 44: `sku text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 45: `title text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 46: `description text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 47: `"itemType" public."StoreItemType" not null default 'DIGITAL',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 48: `"priceAmount" bigint not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 49: `currency text not null default 'IRR',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 50: `"inventoryCount" integer,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 51: `"imageUrl" text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 52: `"sortOrder" integer not null default 0,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 53: `"isActive" boolean not null default true,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 54: `metadata jsonb not null default '{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 55: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 56: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 57: `constraint "StoreItem_priceAmount_nonnegative" check ("priceAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 58: `constraint "StoreItem_inventory_nonnegative" check ("inventoryCount" is null or "inventoryCount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 59: `unique ("storeId", sku)` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 60: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 62: `create table if not exists public."StoreCustomer" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 63: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 64: `"storeId" text not null references public."Store"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 65: `platform text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 66: `"externalUserId" text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 67: `username text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 68: `"displayName" text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 69: `phone text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 70: `metadata jsonb not null default '{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 71: `"lastSeenAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 72: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 73: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 74: `unique ("storeId", platform, "externalUserId")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 75: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 77: `create table if not exists public."StoreCart" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 78: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 79: `"storeId" text not null references public."Store"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 80: `"customerId" text not null references public."StoreCustomer"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 81: `status public."StoreCartStatus" not null default 'ACTIVE',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 82: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 83: `"updatedAt" timestamptz not null default now()` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 84: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 86: `create table if not exists public."StoreCartItem" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 87: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 88: `"cartId" text not null references public."StoreCart"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 89: `"itemId" text not null references public."StoreItem"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 90: `quantity integer not null default 1,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 91: `"unitPriceAmount" bigint not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 92: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 93: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 94: `constraint "StoreCartItem_quantity_positive" check (quantity > 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 95: `constraint "StoreCartItem_unitPrice_nonnegative" check ("unitPriceAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 96: `unique ("cartId", "itemId")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 97: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 99: `create table if not exists public."StoreOrder" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 100: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 101: `"storeId" text not null references public."Store"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 102: `"customerId" text references public."StoreCustomer"(id) on delete set null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 103: `"sourcePlatform" text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 104: `"externalConversationId" text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 105: `status public."StoreOrderStatus" not null default 'NEW',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 106: `"subtotalAmount" bigint not null default 0,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 107: `"discountAmount" bigint not null default 0,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 108: `"totalAmount" bigint not null default 0,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 109: `currency text not null default 'IRR',` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 110: `note text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 111: `"idempotencyKey" text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 112: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 113: `"updatedAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 114: `"paidAt" timestamptz,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 115: `constraint "StoreOrder_subtotal_nonnegative" check ("subtotalAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 116: `constraint "StoreOrder_discount_nonnegative" check ("discountAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 117: `constraint "StoreOrder_total_nonnegative" check ("totalAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 118: `unique ("storeId", "idempotencyKey")` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 119: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 121: `create table if not exists public."StoreOrderItem" (` — این دستور یک جدول جدید در PostgreSQL ایجاد می‌کند.
- خط 122: `id text primary key default gen_random_uuid()::text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 123: `"orderId" text not null references public."StoreOrder"(id) on delete cascade,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 124: `"itemId" text references public."StoreItem"(id) on delete set null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 125: `"titleSnapshot" text not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 126: `"skuSnapshot" text,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 127: `"unitPriceAmount" bigint not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 128: `quantity integer not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 129: `"lineTotalAmount" bigint not null,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 130: `metadata jsonb not null default '{}'::jsonb,` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 131: `"createdAt" timestamptz not null default now(),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 132: `constraint "StoreOrderItem_quantity_positive" check (quantity > 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 133: `constraint "StoreOrderItem_unitPrice_nonnegative" check ("unitPriceAmount" >= 0),` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 134: `constraint "StoreOrderItem_lineTotal_nonnegative" check ("lineTotalAmount" >= 0)` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 135: `);` — این خط بخشی از دستور SQL یا تعریف ساختار/داده دیتابیس است.
- خط 137: `create index if not exists "StoreCategory_storeId_sortOrder_idx" on public."StoreCategory" ("storeId", "sortOrder");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 138: `create index if not exists "StoreItem_storeId_active_sortOrder_idx" on public."StoreItem" ("storeId", "isActive", "sortOrder");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 139: `create index if not exists "StoreItem_categoryId_active_idx" on public."StoreItem" ("categoryId", "isActive");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 140: `create index if not exists "StoreCustomer_storeId_platform_idx" on public."StoreCustomer" ("storeId", platform);` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 141: `create unique index if not exists "StoreCart_customer_active_unique" on public."StoreCart" ("customerId") where status = 'ACTIVE';` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 142: `create index if not exists "StoreCart_storeId_status_idx" on public."StoreCart" ("storeId", status);` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 143: `create index if not exists "StoreOrder_storeId_createdAt_idx" on public."StoreOrder" ("storeId", "createdAt" desc);` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 144: `create index if not exists "StoreOrder_storeId_status_idx" on public."StoreOrder" ("storeId", status);` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 145: `create index if not exists "StoreOrder_customerId_createdAt_idx" on public."StoreOrder" ("customerId", "createdAt" desc);` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 146: `create index if not exists "StoreOrderItem_orderId_idx" on public."StoreOrderItem" ("orderId");` — این دستور Index ایجاد می‌کند تا جستجو/Unique constraint بهینه یا enforce شود.
- خط 148: `alter table public."Store" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 149: `alter table public."StoreCategory" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 150: `alter table public."StoreItem" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 151: `alter table public."StoreCustomer" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 152: `alter table public."StoreCart" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 153: `alter table public."StoreCartItem" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 154: `alter table public."StoreOrder" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 155: `alter table public."StoreOrderItem" enable row level security;` — این دستور ساختار یا Constraintهای یک جدول موجود را تغییر می‌دهد.
- خط 157: `revoke all on table public."Store" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 158: `revoke all on table public."StoreCategory" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 159: `revoke all on table public."StoreItem" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 160: `revoke all on table public."StoreCustomer" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 161: `revoke all on table public."StoreCart" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 162: `revoke all on table public."StoreCartItem" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 163: `revoke all on table public."StoreOrder" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 164: `revoke all on table public."StoreOrderItem" from anon, authenticated;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 166: `grant all on table public."Store" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 167: `grant all on table public."StoreCategory" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 168: `grant all on table public."StoreItem" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 169: `grant all on table public."StoreCustomer" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 170: `grant all on table public."StoreCart" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 171: `grant all on table public."StoreCartItem" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 172: `grant all on table public."StoreOrder" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.
- خط 173: `grant all on table public."StoreOrderItem" to service_role;` — این دستور Permission دسترسی نقش‌های دیتابیس را تنظیم می‌کند.