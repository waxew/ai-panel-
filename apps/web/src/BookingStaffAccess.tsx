/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور { useEffect, useMemo, useState } را از ماژول «react» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { useEffect, useMemo, useState } from 'react';

// راهنما: این Type با نام «Key» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Key='booking'|'customers'|'finance'|'reports'|'services'|'staff'|'settings'|'automations';
// راهنما: این Type با نام «Perms» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Perms=Record<Key,boolean>;
// راهنما: این Type با نام «Row» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Row={id:string;name:string;roleTitle?:string|null;email?:string|null;color:string;access:{userId?:string|null;linkedEmail?:string|null;isEnabled:boolean;appointmentScope:'OWN'|'ALL';permissions:Perms}};
// راهنما: این Type با نام «Data» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Data={ok:boolean;staff:Row[];message?:string};
// راهنما: این دستور متغیر/ثابت «labels» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const labels:Record<Key,string>={booking:'نوبت‌ها',customers:'پرونده مشتری',finance:'مالی',reports:'گزارش‌ها',services:'خدمات',staff:'پرسنل',settings:'تنظیمات',automations:'اتوماسیون'};
// راهنما: این تابع «api» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function api(body?:unknown){/* راهنما: این متغیر «r» نتیجه یا Promise یک درخواست شبکه را نگه می‌دارد. */ const r=await fetch('/api/booking/staff-access',body?{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}:undefined);/* راهنما: این دستور متغیر/ثابت «d» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const d=await r.json().catch(()=>({})) as Data;/* راهنما: این شرط بررسی می‌کند آیا «!r.ok» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!r.ok)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error(d.message||'عملیات انجام نشد.');/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «d» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return d;}
// راهنما: این تابع «BookingStaffAccess» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
export default function BookingStaffAccess(){
 // راهنما: این دستور State محلی React برای «[data,setData]، [selected,setSelected]، [busy,setBusy]، [error,setError]، [email,setEmail]» می‌سازد تا مقدار آن در UI نگهداری و با تغییرش صفحه دوباره Render شود.
 const[data,setData]=useState<Data|null>(null),[selected,setSelected]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[email,setEmail]=useState('');
 // راهنما: این useEffect یک اثر جانبی React را اجرا می‌کند؛ معمولاً برای دریافت داده، افزودن Listener یا هماهنگی با سیستم بیرونی استفاده می‌شود.
 useEffect(()=>{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «void api().then(d=>{setData(d);setSelected(d.staff?.[0]?.id||'')}).catch(e=>setError(e in…». */ void api().then(d=>{/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setData(d)». */ setData(d);/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setSelected(d.staff?.[0]?.id||'')». */ setSelected(d.staff?.[0]?.id||'')}).catch(e=>setError(e instanceof Error?e.message:'خطا'))},[]);
 // راهنما: این دستور مقدار «row» را با useMemo محاسبه و Cache می‌کند تا محاسبه غیرضروری در Renderهای بعدی تکرار نشود.
 const row=useMemo(()=>data?.staff.find(x=>x.id===selected),[data,selected]);
 // راهنما: این useEffect یک اثر جانبی React را اجرا می‌کند؛ معمولاً برای دریافت داده، افزودن Listener یا هماهنگی با سیستم بیرونی استفاده می‌شود.
 useEffect(()=>setEmail(row?.access.linkedEmail||row?.email||''),[row?.id,row?.access.linkedEmail,row?.email]);
 // راهنما: این تابع «run» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
 async function run(body:unknown){/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setBusy(true)». */ setBusy(true);/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setError('')». */ setError('');/* راهنما: این بلوک عملیات دارای احتمال خطا را اجرا می‌کند؛ اگر خطایی رخ دهد بخش catch می‌تواند آن را مدیریت کند و finally در صورت وجود همیشه اجرا می‌شود. */ try{/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setData(await api(body))». */ setData(await api(body))}catch(e){/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setError(e instanceof Error?e.message:'خطا')». */ setError(e instanceof Error?e.message:'خطا')}finally{/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setBusy(false)». */ setBusy(false)}}
 // راهنما: این تابع «edit» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
 function edit(fn:(r:Row)=>Row){/* راهنما: این شرط بررسی می‌کند آیا «!data||!row» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!data||!row)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «بدون مقدار» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return;/* راهنما: این دستور با فراخوانی Setter، State مربوط را تغییر می‌دهد تا داده جدید در UI اعمال شود: «setData({...data,staff:data.staff.map(x=>x.id===row.id?fn(x):x)})». */ setData({...data,staff:data.staff.map(x=>x.id===row.id?fn(x):x)})}
 // راهنما: این تابع «save» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
 async function save(){/* راهنما: این شرط بررسی می‌کند آیا «row» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(row)/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «await run({action:'save_access',staffId:row.id,isEnabled:row.access.isEnabled,appointment…». */ await run({action:'save_access',staffId:row.id,isEnabled:row.access.isEnabled,appointmentScope:row.access.appointmentScope,permissions:row.access.permissions})}
 // راهنما: این شرط بررسی می‌کند آیا «!data» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(!data)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «<main className="sa load" dir="rtl"><style>{css}</style>{error||'در حال با…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return <main className="sa load" dir="rtl"><style>{css}</style>{error||'در حال بارگذاری...'}</main>;
 // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «<main className="sa" dir="rtl"><style>{css}</style><header><div><small>AI …» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
 return <main className="sa" dir="rtl"><style>{css}</style><header><div><small>AI PANEL · STAFF</small><h1>دسترسی پرسنل</h1><p>سطح مشاهده و مدیریت هر پرسنل در ماژول نوبت‌دهی.</p></div><div><a href="/app/booking/staff">پرسنل و ظرفیت</a><a href="/app/booking/tools">ابزارها</a></div></header>{error&&<div className="err">{error}</div>}<section className="layout"><aside>{data.staff.map(x=><button key={x.id} className={selected===x.id?'on':''} onClick={()=>setSelected(x.id)}><i style={{background:x.color}}>{x.name[0]}</i><span><b>{x.name}</b><small>{x.roleTitle||'پرسنل'}</small></span><em className={x.access.isEnabled?'yes':''}>{x.access.isEnabled?'فعال':'خاموش'}</em></button>)}</aside>{row&&<div className="content"><section className="panel top"><div><i style={{background:row.color}}>{row.name[0]}</i><span><small>پروفایل</small><h2>{row.name}</h2><p>{row.roleTitle||'پرسنل'}</p></span></div><label><span><b>فعال بودن دسترسی</b><small>کنترل دسترسی این پرسنل به پنل Booking</small></span><input type="checkbox" checked={row.access.isEnabled} onChange={e=>edit(x=>({...x,access:{...x.access,isEnabled:e.target.checked}}))}/></label></section><section className="panel"><h2>اتصال حساب</h2>{row.access.userId?<div className="linked"><b>حساب متصل است</b><span>{row.access.linkedEmail}</span><button disabled={busy} onClick={()=>void run({action:'unlink_user',staffId:row.id})}>قطع اتصال</button></div>:<div className="link"><input dir="ltr" value={email} onChange={e=>setEmail(e.target.value)} placeholder="staff@example.com"/><button disabled={busy||!email} onClick={()=>void run({action:'link_existing_user',staffId:row.id,email})}>اتصال حساب موجود</button><small>حساب باید از قبل عضو همین Workspace باشد.</small></div>}</section><section className="panel"><h2>محدوده نوبت‌ها</h2><div className="scope"><button className={row.access.appointmentScope==='OWN'?'on':''} onClick={()=>edit(x=>({...x,access:{...x.access,appointmentScope:'OWN'}}))}><b>فقط نوبت‌های خودش</b><small>فقط نوبت‌های اختصاص داده شده به این پرسنل.</small></button><button className={row.access.appointmentScope==='ALL'?'on':''} onClick={()=>edit(x=>({...x,access:{...x.access,appointmentScope:'ALL'}}))}><b>همه نوبت‌ها</b><small>مشاهده و مدیریت نوبت تمام پرسنل.</small></button></div></section><section className="panel"><h2>بخش‌های مجاز</h2><div className="perms">{(Object.keys(labels) as Key[]).map(k=><label key={k}><span><b>{labels[k]}</b><small>{row.access.permissions[k]?'فعال':'غیرفعال'}</small></span><input type="checkbox" checked={row.access.permissions[k]} onChange={e=>edit(x=>({...x,access:{...x.access,permissions:{...x.access.permissions,[k]:e.target.checked}}}))}/></label>)}</div></section><button className="save" disabled={busy} onClick={()=>void save()}>{busy?'در حال ذخیره...':'ذخیره دسترسی‌ها'}</button></div>}</section></main>;
}
// راهنما: این دستور متغیر/ثابت «css» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const css=`*{box-sizing:border-box}.sa{min-height:100vh;background:#f5f7fb;color:#172033;font-family:Inter,Vazirmatn,Tahoma,Arial,sans-serif;padding:26px 24px 100px}.sa button,.sa input{font:inherit}.sa>header{max-width:1220px;margin:auto auto 15px;display:flex;justify-content:space-between;align-items:flex-end}.sa header small{font-size:8px;color:#695ddc;font-weight:900}.sa h1{font-size:27px;margin:5px 0}.sa header p{font-size:10px;color:#818b9a;margin:0}.sa header>div:last-child{display:flex;gap:7px}.sa header a{text-decoration:none;color:#596577;background:#fff;border:1px solid #e1e5ec;border-radius:9px;padding:9px 11px;font-size:9px}.err{max-width:1220px;margin:0 auto 10px;padding:10px;background:#fff1f2;color:#bd4052;border:1px solid #ffd2d8;border-radius:10px;font-size:9px}.layout{max-width:1220px;margin:auto;display:grid;grid-template-columns:230px 1fr;gap:11px}.layout>aside,.panel{background:#fff;border:1px solid #e4e8ef;border-radius:15px}.layout>aside{padding:10px;height:max-content}.layout>aside button{border:0;background:transparent;width:100%;display:grid;grid-template-columns:34px 1fr auto;gap:8px;align-items:center;padding:9px;border-radius:10px;text-align:right;color:#273246;cursor:pointer}.layout>aside button.on{background:#f0eeff}.layout>aside i,.top>div>i{width:34px;height:34px;border-radius:9px;color:#fff;display:grid;place-items:center;font-style:normal;font-weight:900}.layout>aside span{display:flex;flex-direction:column}.layout>aside b{font-size:9px}.layout>aside small{font-size:7px;color:#8994a5}.layout>aside em{font-size:7px;font-style:normal;background:#f0f2f5;color:#929baa;padding:5px 7px;border-radius:99px}.layout>aside em.yes{background:#e8f8ef;color:#278459}.content{display:grid;gap:10px}.panel{padding:15px}.panel h2{font-size:12px;margin:0 0 12px}.top{display:flex;justify-content:space-between;align-items:center}.top>div{display:flex;gap:9px;align-items:center}.top>div>i{width:45px;height:45px}.top span{display:flex;flex-direction:column}.top h2{margin:3px 0}.top p,.top small{font-size:8px;color:#8893a3;margin:0}.top>label{display:flex;gap:12px;align-items:center}.top>label b{font-size:9px}.top>label small{font-size:7px}.sa input[type=checkbox]{width:36px;height:20px;accent-color:#6256dd}.linked{display:grid;grid-template-columns:1fr auto;gap:4px}.linked b,.linked span{font-size:9px}.linked span{color:#7e8999}.linked button{grid-row:1/3;grid-column:2;border:1px solid #efc7cf;background:#fff4f5;color:#bd4356;border-radius:8px;padding:7px}.link{display:grid;grid-template-columns:1fr 150px;gap:7px}.link input{border:1px solid #e1e5ec;border-radius:8px;padding:9px}.link button{border:0;background:#222a38;color:#fff;border-radius:8px;font-size:8px}.link small{grid-column:1/-1;font-size:7px;color:#8994a4}.scope{display:grid;grid-template-columns:1fr 1fr;gap:8px}.scope button{border:1px solid #e2e6ed;background:#fafbfc;color:#273246;border-radius:10px;padding:12px;text-align:right}.scope button.on{border-color:#8d83e8;background:#f2f0ff}.scope b{display:block;font-size:9px}.scope small{display:block;font-size:7px;color:#8994a4;margin-top:4px}.perms{display:grid;grid-template-columns:1fr 1fr;gap:7px}.perms label{display:flex;justify-content:space-between;align-items:center;border:1px solid #e7eaf0;border-radius:9px;padding:10px}.perms span{display:flex;flex-direction:column}.perms b{font-size:9px}.perms small{font-size:7px;color:#8a95a5}.save{border:0;background:#6256dd;color:#fff;border-radius:10px;padding:12px;font-size:9px;font-weight:900}.load{display:grid;place-items:center}@media(max-width:800px){.sa{padding:15px 10px 100px}.sa>header{display:block}.sa header>div:last-child{margin-top:9px}.layout{grid-template-columns:1fr}.layout>aside{display:flex;overflow:auto}.layout>aside button{min-width:170px}.top{display:block}.top>label{margin-top:13px;justify-content:space-between}.scope,.perms,.link{grid-template-columns:1fr}.link small{grid-column:1}}`;
