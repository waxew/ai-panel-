/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور { useEffect, useMemo, useState } را از ماژول «react» وارد می‌کند تا در این فایل قابل استفاده باشد.
import { useEffect, useMemo, useState } from 'react';

// راهنما: این Type با نام «Appointment» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Appointment={id:string;serviceId:string;staffId:string;customerId:string;startsAt:string;status:string;amount:number|string;paidAmount:number|string;BookingCustomer?:{fullName:string}|null;BookingService?:{title:string}|null;BookingStaff?:{name:string}|null};
// راهنما: این Type با نام «Rank» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Rank={id:string;title?:string;name?:string;color:string;count:number;revenue:number};
// راهنما: این Type با نام «Data» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
type Data={ok:boolean;message?:string;summary:{income:number;expense:number;net:number;receivable:number;monthIncome:number;monthExpense:number;monthNet:number};appointments:Appointment[];byService:Rank[];byStaff:Rank[]};
// راهنما: این دستور متغیر/ثابت «nf» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const nf=new Intl.NumberFormat('fa-IR');/* راهنما: این دستور متغیر/ثابت «money» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const money=(v:number|string)=>`${nf.format(Number(v||0))} ریال`;
// راهنما: این تابع «load» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
async function load(){/* راهنما: این متغیر «r» نتیجه یا Promise یک درخواست شبکه را نگه می‌دارد. */ const r=await fetch('/api/booking/finance?mode=reports');/* راهنما: این دستور متغیر/ثابت «d» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const d=await r.json().catch(()=>({})) as Data;/* راهنما: این شرط بررسی می‌کند آیا «!r.ok» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!r.ok)/* راهنما: این دستور عمداً یک خطا ایجاد می‌کند تا اجرای مسیر فعلی متوقف و کنترل خطا به لایه بالاتر منتقل شود. */ throw new Error(d.message||'گزارش در دسترس نیست.');/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «d» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return d;}
// راهنما: این تابع «BookingReports» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
export default function BookingReports(){
 // راهنما: این دستور State محلی React برای «[data,setData]، [error,setError]، [range,setRange]» می‌سازد تا مقدار آن در UI نگهداری و با تغییرش صفحه دوباره Render شود.
 const[data,setData]=useState<Data|null>(null),[error,setError]=useState(''),[range,setRange]=useState<'30'|'90'|'365'>('30');
 // راهنما: این useEffect یک اثر جانبی React را اجرا می‌کند؛ معمولاً برای دریافت داده، افزودن Listener یا هماهنگی با سیستم بیرونی استفاده می‌شود.
 useEffect(()=>{/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «void load().then(setData).catch(e=>setError(e instanceof Error?e.message:'خطا'))». */ void load().then(setData).catch(e=>setError(e instanceof Error?e.message:'خطا'));},[]);
 // راهنما: این دستور مقدار «filtered» را با useMemo محاسبه و Cache می‌کند تا محاسبه غیرضروری در Renderهای بعدی تکرار نشود.
 const filtered=useMemo(()=>{/* راهنما: این شرط بررسی می‌کند آیا «!data» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود. */ if(!data)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «[]» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return[];/* راهنما: این دستور متغیر/ثابت «after» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const after=Date.now()-Number(range)*86400000;/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «data.appointments.filter(a=>new Date(a.startsAt).getTime()>=after)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return data.appointments.filter(a=>new Date(a.startsAt).getTime()>=after);},[data,range]);
 // راهنما: این دستور مقدار «stats» را با useMemo محاسبه و Cache می‌کند تا محاسبه غیرضروری در Renderهای بعدی تکرار نشود.
 const stats=useMemo(()=>{/* راهنما: این دستور متغیر/ثابت «total، done، cancelled، noShow» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const total=filtered.length,done=filtered.filter(x=>x.status==='DONE').length,cancelled=filtered.filter(x=>x.status==='CANCELLED').length,noShow=filtered.filter(x=>x.status==='NO_SHOW').length;/* راهنما: این دستور متغیر/ثابت «value، paid» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const value=filtered.reduce((s,x)=>s+Number(x.amount||0),0),paid=filtered.reduce((s,x)=>s+Number(x.paidAmount||0),0);/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «{total,done,cancelled,noShow,value,paid,completion:total?Math.round(done/t…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return{total,done,cancelled,noShow,value,paid,completion:total?Math.round(done/total*100):0,cancelRate:total?Math.round(cancelled/total*100):0};},[filtered]);
 // راهنما: این دستور مقدار «daily» را با useMemo محاسبه و Cache می‌کند تا محاسبه غیرضروری در Renderهای بعدی تکرار نشود.
 const daily=useMemo(()=>{/* راهنما: این دستور متغیر/ثابت «m» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const m=new Map<string,number>();/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «filtered.forEach(a=>{const k=new Date(a.startsAt).toISOString().slice(0,10);m.set(k,(m.ge…». */ filtered.forEach(a=>{/* راهنما: این دستور متغیر/ثابت «k» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const k=new Date(a.startsAt).toISOString().slice(0,10);/* راهنما: این دستور یک عملیات اجرایی انجام می‌دهد: «m.set(k,(m.get(k)||0)+1)». */ m.set(k,(m.get(k)||0)+1)});/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «[...m.entries()].sort().slice(-14)» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return[...m.entries()].sort().slice(-14);},[filtered]);
 // راهنما: این دستور متغیر/ثابت «max» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
 const max=Math.max(1,...daily.map(x=>x[1]));
 // راهنما: این شرط بررسی می‌کند آیا «!data» برقرار است؛ فقط در صورت درست بودن، شاخه مربوط اجرا می‌شود.
 if(!data)/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «<main className="br loading" dir="rtl"><style>{styles}</style>{error||'در …» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return <main className="br loading" dir="rtl"><style>{styles}</style>{error||'در حال آماده‌سازی گزارش...'}</main>;
 // راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «<main className="br" dir="rtl"><style>{styles}</style><header><div><small>…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد.
 return <main className="br" dir="rtl"><style>{styles}</style><header><div><small>AI PANEL · Reports</small><h1>گزارش سیستم</h1><p>عملکرد نوبت‌ها، خدمات، پرسنل و وضعیت مالی در یک صفحه.</p></div><div><select value={range} onChange={e=>setRange(e.target.value as '30'|'90'|'365')}><option value="30">۳۰ روز اخیر</option><option value="90">۹۰ روز اخیر</option><option value="365">یک سال اخیر</option></select><a href="/app/booking/tools">ابزارها</a></div></header>{error&&<div className="err">{error}</div>}
 <section className="kpis"><Card label="کل نوبت‌ها" value={nf.format(stats.total)} hint={`${nf.format(stats.completion)}٪ تکمیل`}/><Card label="لغو شده" value={nf.format(stats.cancelled)} hint={`${nf.format(stats.cancelRate)}٪ نرخ لغو`}/><Card label="عدم حضور" value={nf.format(stats.noShow)} hint="No-show"/><Card label="ارزش نوبت‌ها" value={money(stats.value)} hint={`دریافتی ${money(stats.paid)}`}/></section>
 <section className="chart panel"><div><h2>روند نوبت‌های اخیر</h2><small>۱۴ روز آخر در بازه انتخابی</small></div><div className="bars">{daily.length?daily.map(([d,v])=><span key={d}><i style={{height:`${Math.max(8,(v/max)*100)}%`}}/><small>{new Date(d).toLocaleDateString('fa-IR',{month:'numeric',day:'numeric'})}</small><b>{nf.format(v)}</b></span>):<p>داده‌ای ثبت نشده است.</p>}</div></section>
 <section className="two"><Rank title="عملکرد خدمات" rows={data.byService.map(x=>({id:x.id,name:x.title||'خدمت',color:x.color,count:filtered.filter(a=>a.serviceId===x.id).length,revenue:filtered.filter(a=>a.serviceId===x.id).reduce((s,a)=>s+Number(a.paidAmount||0),0)}))}/><Rank title="عملکرد پرسنل" rows={data.byStaff.map(x=>({id:x.id,name:x.name||'پرسنل',color:x.color,count:filtered.filter(a=>a.staffId===x.id).length,revenue:filtered.filter(a=>a.staffId===x.id).reduce((s,a)=>s+Number(a.paidAmount||0),0)}))}/></section>
 <section className="panel"><h2>آخرین رویدادهای نوبت</h2><div className="events">{filtered.slice(0,20).map(a=><div key={a.id}><span><b>{a.BookingCustomer?.fullName||'مشتری'}</b><small>{a.BookingService?.title||'خدمت'} · {a.BookingStaff?.name||'پرسنل'}</small></span><span className={`status ${a.status.toLowerCase()}`}>{label(a.status)}</span><span><b>{money(a.amount)}</b><small>{new Date(a.startsAt).toLocaleString('fa-IR')}</small></span></div>)}</div></section>
 </main>;
}
// راهنما: این تابع «label» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function label(v:string){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «v==='DONE'?'انجام‌شده':v==='CONFIRMED'?'تأییدشده':v==='PENDING'?'در انتظار…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return v==='DONE'?'انجام‌شده':v==='CONFIRMED'?'تأییدشده':v==='PENDING'?'در انتظار':v==='CANCELLED'?'لغوشده':v==='NO_SHOW'?'عدم حضور':v}
// راهنما: این تابع «Card» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function Card({label,value,hint}:{label:string;value:string;hint:string}){/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «<article><small>{label}</small><strong>{value}</strong><span>{hint}</span>…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return <article><small>{label}</small><strong>{value}</strong><span>{hint}</span></article>}
// راهنما: این تابع «Rank» یک بخش مستقل از منطق برنامه را تعریف می‌کند؛ ورودی‌ها را می‌گیرد و منطق داخل بدنه را اجرا می‌کند.
function Rank({title,rows}:{title:string;rows:Array<{id:string;name:string;color:string;count:number;revenue:number}>}){/* راهنما: این دستور متغیر/ثابت «sorted» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const sorted=[...rows].sort((a,b)=>b.count-a.count);/* راهنما: این دستور متغیر/ثابت «max» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد. */ const max=Math.max(1,...sorted.map(x=>x.count));/* راهنما: این Return اجرای تابع را در این نقطه تمام می‌کند و «<section className="panel"><h2>{title}</h2><div className="rank">{sorted.s…» را به فراخواننده برمی‌گرداند؛ در کامپوننت React می‌تواند UI خروجی باشد. */ return <section className="panel"><h2>{title}</h2><div className="rank">{sorted.slice(0,7).map(x=><div key={x.id}><span><i style={{background:x.color}}/><b>{x.name}</b><small>{nf.format(x.count)} نوبت</small></span><div><i style={{width:`${Math.max(4,x.count/max*100)}%`}}/><b>{money(x.revenue)}</b></div></div>)}</div></section>}
// راهنما: این دستور متغیر/ثابت «styles» را تعریف می‌کند و مقدار موردنیاز این بخش از برنامه را نگه می‌دارد.
const styles=`
*{box-sizing:border-box}.br{min-height:100vh;background:#f5f7fb;color:#172033;font-family:Inter,Vazirmatn,Tahoma,Arial,sans-serif;padding:26px 24px 100px}.br>header{max-width:1250px;margin:auto auto 16px;display:flex;justify-content:space-between;align-items:flex-end;gap:20px}.br header small{font-size:9px;color:#4c79e8;font-weight:900}.br h1{font-size:27px;margin:5px 0}.br header p{margin:0;color:#7f8998;font-size:10px}.br header>div:last-child{display:flex;gap:7px}.br select,.br header a{border:1px solid #e0e5ed;background:#fff;color:#586477;border-radius:9px;padding:9px 11px;font-size:9px;text-decoration:none}.err{max-width:1250px;margin:0 auto 12px;background:#fff1f2;color:#bd4052;border:1px solid #ffd2d8;border-radius:10px;padding:10px;font-size:9px}.kpis,.panel,.two{max-width:1250px;margin-left:auto;margin-right:auto}.kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:10px}.kpis article,.panel{background:#fff;border:1px solid #e4e8ef;border-radius:15px}.kpis article{padding:15px;display:flex;flex-direction:column}.kpis small{font-size:8px;color:#8792a2}.kpis strong{font-size:17px;margin:8px 0}.kpis span{font-size:8px;color:#657188}.panel{padding:15px;margin-bottom:10px}.panel h2{font-size:12px;margin:0}.chart>div:first-child{display:flex;justify-content:space-between;align-items:center}.chart>div:first-child small{font-size:8px;color:#8c97a6}.bars{height:190px;display:flex;align-items:flex-end;gap:7px;padding-top:20px}.bars>span{height:100%;flex:1;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;gap:5px;position:relative}.bars>span>i{width:70%;max-width:30px;background:#5d83e8;border-radius:7px 7px 2px 2px}.bars small{font-size:7px;color:#8a95a4}.bars b{font-size:7px}.two{display:grid;grid-template-columns:1fr 1fr;gap:10px}.rank>div{padding:9px 0;border-top:1px solid #eef1f5}.rank>div>span{display:grid;grid-template-columns:8px 1fr auto;gap:7px;align-items:center}.rank>div>span i{width:8px;height:8px;border-radius:50%}.rank b{font-size:9px}.rank small{font-size:8px;color:#8d97a6}.rank>div>div{height:18px;background:#f0f2f6;border-radius:99px;margin-top:6px;position:relative;overflow:hidden}.rank>div>div i{display:block;height:100%;background:#5d83e8;border-radius:99px}.rank>div>div b{position:absolute;left:7px;top:3px;font-size:7px}.events>div{display:grid;grid-template-columns:1fr 90px 180px;gap:10px;align-items:center;padding:10px 0;border-top:1px solid #eef1f5}.events span{display:flex;flex-direction:column}.events b{font-size:9px}.events small{font-size:8px;color:#8c97a6;margin-top:3px}.status{display:inline-grid!important;place-items:center;border-radius:999px;padding:6px 8px;font-size:8px;font-weight:800}.status.done{background:#e8f8ef;color:#27885a}.status.confirmed{background:#eef3ff;color:#4266c9}.status.pending{background:#fff7df;color:#9b771a}.status.cancelled{background:#fff0f2;color:#bc4256}.status.no_show{background:#f1f2f4;color:#677181}.loading{display:grid;place-items:center;font-size:10px}
@media(max-width:850px){.br{padding:16px 10px 100px}.br>header{display:block}.br header>div:last-child{margin-top:10px}.kpis{grid-template-columns:1fr 1fr}.two{grid-template-columns:1fr}.events>div{grid-template-columns:1fr auto}.events>div>span:last-child{grid-column:1/-1}.bars{height:160px;gap:3px}}
`;
