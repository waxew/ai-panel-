# راهنمای فارسی `package.json` ریشه

فایل اصلی: `package.json`

> `package.json` از فرمت JSON استفاده می‌کند و JSON استاندارد اجازه `//` یا `#` برای کامنت داخل فایل را نمی‌دهد. اگر توضیح فارسی را مستقیماً داخل آن قرار دهیم، `npm` و `pnpm` ممکن است فایل را نامعتبر تشخیص دهند. به همین دلیل توضیحات این فایل کنار خودش نگهداری می‌شود.

## `name`
نام پکیج اصلی/ریشه Monorepo را مشخص می‌کند.

## `private`
وقتی `true` باشد جلوی Publish تصادفی پکیج ریشه در Registry عمومی npm را می‌گیرد.

## `version`
نسخه فعلی پکیج ریشه پروژه است.

## `packageManager`
مشخص می‌کند مدیر پکیج استاندارد پروژه `pnpm` و چه نسخه‌ای از آن است؛ Corepack نیز می‌تواند از این مقدار استفاده کند.

## `scripts`
هر کلید داخل `scripts` یک دستور آماده است که با `pnpm <نام>` یا در بعضی موارد `pnpm run <نام>` اجرا می‌شود. این Scriptها معمولاً اجرای محیط توسعه، Build، Prisma، Typecheck یا Deployment بخش‌های مختلف Monorepo را به هم متصل می‌کنند.

## `dependencies`
کتابخانه‌هایی هستند که کد پروژه برای کارکرد Runtime به آن‌ها وابسته است. نسخه‌های آن‌ها توسط مدیر پکیج نصب می‌شوند و محتوای واقعی‌شان داخل `node_modules` قرار می‌گیرد.

## `devDependencies`
ابزارهایی هستند که عمدتاً برای توسعه، TypeScript، Build، CLIها، تست یا Generation استفاده می‌شوند و معمولاً بخشی از منطق Business برنامه نیستند.

## ارتباط با `node_modules`
`package.json` فقط اعلام می‌کند چه پکیج‌هایی لازم داریم. خود کد آن پکیج‌ها با `pnpm install` دانلود و در `node_modules` قرار می‌گیرد. بنابراین `node_modules` کد نوشته‌شده توسط پروژه AI Panel نیست و نباید برای شخصی‌سازی پروژه مستقیماً ویرایش شود.

## ارتباط با `pnpm-lock.yaml`
Lockfile نسخه دقیق Dependencyهای نصب‌شده و Dependencyهای وابسته را ثبت می‌کند تا نصب روی سیستم‌های مختلف تا حد ممکن یکسان باشد. این فایل عمدتاً توسط pnpm تولید و به‌روزرسانی می‌شود، نه اینکه خط‌به‌خط دستی نوشته شود.
