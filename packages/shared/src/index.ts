/**
 * AI-PANEL-FA-INLINE-GUIDE
 * این فایل توسط راهنمای فارسی AI Panel مستندسازی شده است.
 * کامنت‌های «راهنما» توضیح می‌دهند دستور یا بلوک بعدی چه نقشی دارد.
 * این توضیحات بخشی از Runtime نیستند و JavaScript آن‌ها را اجرا نمی‌کند.
 */
// راهنما: این دستور بخشی از کد را Export می‌کند تا فایل‌ها یا پکیج‌های دیگر بتوانند از آن استفاده کنند.
export * from './modules';
// راهنما: این دستور بخشی از کد را Export می‌کند تا فایل‌ها یا پکیج‌های دیگر بتوانند از آن استفاده کنند.
export * from './providers';
// راهنما: این دستور بخشی از کد را Export می‌کند تا فایل‌ها یا پکیج‌های دیگر بتوانند از آن استفاده کنند.
export * from './bot-commerce';

// راهنما: این Type با نام «BotConnectionStatus» شکل و مقادیر مجاز داده را برای TypeScript مشخص می‌کند و در زمان اجرا کد مستقلی تولید نمی‌کند.
export type BotConnectionStatus = 'pending' | 'active' | 'invalid' | 'disabled';
